# Migration to React — Complete

The frontend was fully migrated from plain HTML/JS to React + Vite as of April 2026. The legacy frontend no longer exists in this repository.

**Current stack:** React 18 + Vite + React Router + Context API  
**Entry point:** `frontend-react/`  
**Dev server:** `http://localhost:3001`

See `docs/frontend-architecture.md` for the current frontend architecture.
