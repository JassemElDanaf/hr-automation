# Diyar HR Automation — Project Memory

Persistent source of truth for the project. Read this before making changes.

> **Project status (June 2026):** Demo-ready, fully local. All six workflows functional end to end. Nav is 7 tabs: Dashboard · CV Pool · Job Openings · CV Evaluation · Shortlist · Emails · Interview (the Interview tab has Setup / Question Bank / Candidate Prep / Results sub-tabs — Results is the former AI Interviews page). A progress report for Diyar management lives at `report/report.pdf` (LaTeX source at `report/report.tex`, compiled with MiKTeX).

---

## 1. Project Overview

**Diyar HR Automation** is a local-first, demo-grade HR pipeline that an HR user drives from a single-page web UI. The backend is a collection of n8n workflows that expose webhooks, persist to PostgreSQL, and call a local Ollama model for AI work. Nothing in this project is a cloud service — everything runs on the HR user's laptop.

**What each module does**

| Module | Job |
|--------|-----|
| Frontend — React (`frontend-react/`) | React + Vite app. 9 page components across 7 nav tabs, shared state via Context, react-router. **The only frontend.** Vite dev server proxies `/webhook`→n8n and `/recording`→recording sidecar so candidate pages work same-origin. |
| n8n workflows (`workflows/`) | One JSON per phase. Webhook-based HTTP API. Talks to PostgreSQL + Ollama + SMTP sidecar. |
| PostgreSQL (Docker) | `hr_automation` database. Schema in `db/schema.sql`, migrations in `db/migrations/`. |
| Ollama (local, port 11434) | Runs `qwen3:4b`. Used for JD generation, criteria generation, CV scoring. |
| SMTP sidecar (`scripts/smtp_server.py`) | Tiny Python HTTP server on `127.0.0.1:8901`. n8n POSTs here, it relays via `smtplib`. Generates a Message-ID per send and returns it so the outbound `email_log` row stores it for later in-reply-to matching. |
| IMAP sidecar (`scripts/imap_server.py`) | Tiny Python daemon on `127.0.0.1:8902`. Polls the configured Gmail mailbox every `IMAP_POLL_SEC` for UNSEEN messages, parses each, and POSTs the payload to n8n's `/inbound-email` webhook so the reply lands in `email_log` with `direction='inbound'`, threaded to the original outbound row by Message-ID. Without `IMAP_HOST` set the daemon stays running but skips polling. |
| Launcher (`start.sh` / `launch.bat`) | Brings up everything in the right order (Docker → Postgres → Ollama → SMTP → n8n → React frontend) and opens http://localhost:3001. n8n auth is disabled via env vars. All data lives on **D:\\** — see §11 Drive Layout. |

**How the pieces fit**

```
Browser (http://localhost:3001)
    |
    +-- fetch ---> n8n webhooks (http://localhost:5678/webhook/...)
                      |
                      +-- Postgres queries ---> hr-postgres container (:5432)
                      +-- HTTP POST ---------> Ollama (:11434)
                      +-- HTTP POST ---------> SMTP sidecar (:8901) ---> SMTP provider

IMAP sidecar (:8902, polling) ---> IMAP server (Gmail)
                                |
                                +-- HTTP POST ---------> n8n /inbound-email
                                                            |
                                                            +-- match in_reply_to → message_id
                                                            +-- INSERT email_log direction='inbound'
```

---

## 2. Final Phase Order

Phases are numbered by **user flow**, not build order. Current nav (7 tabs):

1. **Dashboard** — landing page, KPIs across all jobs
2. **Job Openings** — create + manage JDs (AI / manual / upload)
3. **CV Pool** (`/talent-pool`, TalentPool.jsx) — Ctrl+F-style search across every uploaded CV (all candidates, every job, interviewed or not); inline PDF dropdown; one-click shortlist
4. **CV Evaluation** — 4-step wizard (select job → criteria → upload CVs → run + view results)
5. **Shortlist** — track status: shortlisted → interviewed → hired, or rejected
6. **Interview** (`/live-interview`, LiveInterview.jsx) — sub-tabs: **Setup** (build questions + generate candidate link), **Question Bank**, **Candidate Prep**, **Results** (embedded AIInterviews.jsx: scores, recordings with synced question overlay, transcripts, Re-evaluate, Email HM with attachments, Teams .vtt import)
7. **Emails** — history of every email attempt with SMTP health

Workflow files on disk still use their original folder names (`phase1-job-opening/`, etc.) because renaming would break import scripts. The **workflow `name` field + tags inside each JSON** use the new numbering. `docs/n8n.md` has the full mapping.

---

## 3. Architecture

### Frontend (React — the only frontend)
- `frontend-react/` — React + Vite app
- 9 page components: Dashboard, JobOpenings, CVEvaluation, Shortlist, Emails, TalentPool, LiveInterview, AIInterviews, CandidateInterview (candidate-facing, no HR chrome)
- State: React Context API (`selectedJob` + `uiState`), localStorage persistence for selected job
- Routing: react-router-dom with URL paths (`/`, `/talent-pool` (CV Pool), `/jobs`, `/cv-eval`, `/shortlist`, `/emails`, `/live-interview` (the "Interview" tab: Setup / Question Bank / Candidate Prep / Results sub-tabs — Results embeds AIInterviews; `/ai-interviews` redirects there), plus standalone `/interview/:token` for candidates)
- API: `services/api.js` reads `VITE_API_URL` from `.env` → n8n webhooks
- PDF parsing: `pdfjs-dist` (bundled, not CDN)
- Charts: `chart.js` + `react-chartjs-2`
- Dev server: `npm run dev` on port 3001
- Full architecture: `docs/frontend-architecture.md`

> The legacy single-file HTML SPA (`frontend/index.html`) was deleted on 2026-04-26 — there is no fallback frontend. If you find references to port 3000 or `frontend/`, they are stale and should be removed. Always work in `frontend-react/`.

### Backend / API
No custom backend — the "backend" is n8n. Each HTTP endpoint is a webhook node → pipeline of Code / Postgres / HTTP Request nodes → `respondToWebhook` node.

### n8n Workflows (`workflows/`)
| Folder | New Phase | Internal Name | What it does |
|--------|-----------|---------------|--------------|
| `phase5-dashboard/` | **Phase 1** | `Phase 1 - Dashboard` | `/dashboard-candidates`, `/dashboard-shortlist`, `/talent-pool` |
| `phase1-job-opening/` | **Phase 2** | `Phase 2 - Job Openings` | CRUD for `/job-openings`, toggle active, list, get, `/job-opening-update` |
| `phase2-cv-evaluation/` | **Phase 3** | `Phase 3 - CV Evaluation` | `/cv-submit`, `/cv-evaluate`, `/criteria-sets`, `/generate-criteria`, `/candidates`, `/evaluations`, `/cv-file`, `/generate-interview-questions` |
| `phase3-shortlist/` | **Phase 4** | `Phase 4 - Shortlist` | `/shortlist` GET/POST, `/shortlist-update`, `/add-to-shortlist`, `/update-shortlist-status`, `/remove-from-shortlist` |
| `phase4-email/` | **Phase 5** | `Phase 5 - Emails` | `/send-email`, `/email-history` |
| `phase6-live-interview/` | **Phase 6** | `Phase 6 - Live Interview` | `/interview/jobs`, `/interview/candidates`, `/interview/question-bank`, `/generate-interview-questions`, `/candidate-questions` (POST/GET), `/candidate-questions-list` |

### Database (`db/`)
- `schema.sql` — initial Phase 2 `job_openings` table
- `migrations/001-phase2-cv-evaluation.sql` — `candidates`, `evaluations`
- `migrations/002-criteria-sets.sql` — `criteria_sets`
- `migrations/003-phase3-shortlist.sql` — `shortlist`
- `migrations/004-phase4-email-log.sql` — `email_log`
- `migrations/005-phase4-smtp.sql` — adds `error_message` column, widens status enum
- `migrations/006-criteria-items.sql` — adds `criteria_items JSONB` column to `criteria_sets` for per-item required/optional + weight
- `migrations/007-cv-file-data.sql` — adds `cv_file_name`, `cv_file_data` (base64 TEXT), `cv_file_mime` columns to `candidates` so the original PDF is retrievable post-upload
- `migrations/008-recommendation-email-type.sql` — relaxes `email_log_email_type_check` to allow `'recommendation'` (HR → hiring manager email)
- `migrations/009-email-direction.sql` — adds `direction` (`outbound`/`inbound` CHECK, default `'outbound'`), `message_id`, `in_reply_to` columns + matching indexes on `email_log` so inbound replies pulled by the IMAP sidecar can be threaded to the parent outbound row
- `migrations/013-question-bank.sql` — `question_bank` table for reusable interview questions
- `migrations/014-candidate-prepared-questions.sql` — `candidate_prepared_questions` table with UNIQUE(candidate_id, job_opening_id) for persisting per-candidate interview prep (questions, notes, meeting details) so they follow the candidate everywhere

Full table map is in `docs/database.md`.

### Docker
The only container is PostgreSQL (`hr-postgres`). There is **no `docker-compose.yml`** — the container is created with a single `docker run` command documented in `start.sh` line 54 and in `docs/docker.md`.

### Report (`report/`)
- `report.tex` — LaTeX source of the stakeholder progress report (12pt Times New Roman via `mathptmx`, cover page with Diyar logo).
- `report.pdf` — compiled output sent to Diyar management.
- `images/` — screenshots (Dashboard, Jobs, Criteria, Upload, Results, Shortlist, Emails, Docker Desktop, n8n) and `diyar-logo.jpg`, all with clean filenames so LaTeX handles them without space-escaping issues.
- Compiled with MiKTeX on `C:\Users\Jasse\AppData\Local\Programs\MiKTeX\miktex\bin\x64\pdflatex.exe`. Run `pdflatex` twice after TOC or label changes.

### Environment / Config
- `.env` (gitignored) holds SMTP credentials + any overrides. Loaded by `start.sh`.
- `.env.example` is the template — copy to `.env` and fill in real values.
- n8n's own DB (`E:\n8n\database.sqlite`) stores imported workflow state separately. Set via `N8N_USER_FOLDER=/e/n8n` in `start.sh`.

---

## 4. Core UX / Product Rules

These rules are **load-bearing**. Breaking them has caused real bugs.

### Cross-tab Job Context
0. **One selected job, visible everywhere.** CV Evaluation, Shortlist, and Emails all operate on the same `globalSelectedJob`. The header renders a "Current Job: {title} · {department}" badge that reflects it, and every per-tab selector mirrors it. Any selection in one tab propagates to the others instantly — the user never has to re-select a job after switching tabs. Persisted in `localStorage` under `hr_selected_job` so selection survives reloads. See §5a for the full lifecycle.

### CV Evaluation (Phase 3)
1. **Existing jobs must be non-linear.** If a job already has state, the stepper lets the user jump between Set Criteria / Upload CVs / Results freely. Don't force them back through Step 1.
2. **Set Criteria is always reachable once a job is selected.** It's an editable workspace, not a one-time gate. Entering it with empty criteria is valid.
3. **Results is directly reachable when state allows.** If `has_evaluations`, clicking Step 4 (or the "View Results" shortcut) opens Results immediately and loads the scored table.
4. **Criteria generation is a manual trigger only.** Never auto-generate on page load, job selection, tab switch, or weight change. The user clicks "Generate Criteria" explicitly.
5. **Generation flow is input → generate → edit.** The "Additional Context" textarea collects HR intent, the button runs Ollama with current weights, the output appears in an editable textarea.
6. **Generate Criteria button lives AFTER the scoring weights.** The button reads the current weights to inform the prompt.
7. **Candidate row actions are state-dependent.** The actions column shows different content based on the candidate's shortlist status:
   - **Pending** (no shortlist entry): `Run Evaluation` (if unevaluated) · `Details` / `View CV` · `Shortlist` · `Reject`
   - **Shortlisted** (or interviewed/hired): a centered green pill badge showing the status (e.g. "\u2713 Shortlisted") + an Archive button.
   - **Rejected**: a centered red pill badge "\u2717 Rejected" + an Archive button.
   Status is fetched from `/shortlist?job_id=N` on Step 4 load, so it persists across page refreshes. On shortlist/reject, the local `shortlistMap` state is updated immediately (no full reload needed) and the candidate is added to `retainedInView` so the row stays visible in the current filter until the user switches filters or reloads — status changes feel like updates, not instant disappearances. A pop animation plays on the badge when the state first changes. Rows also get a subtle tint: light red for rejected, light green for shortlisted. Status badges are centered horizontally in the Actions column via `.actions-container:has(.status-action-badge) { justify-content: center }`. Actions are always rendered in a `<div class="actions-container">` inside a `<td class="actions-cell">`. **Toast styling:** shortlist uses green (`success`), reject uses red (`error`), archive uses blue (`info`). Positive and destructive actions must be visually distinct.
   **Run Evaluation button:** pre-checks for unevaluated candidates before calling the backend. If all candidates are already evaluated, the button is disabled and shows "\u2713 All Evaluated". Otherwise it shows the unevaluated count. Error messages are actionable: "no candidates uploaded", "all already evaluated", "Ollama may not have responded", "cannot reach n8n", etc.
8. **Reject is candidate-level, not evaluation-level.** Every pending candidate row has a Reject button regardless of whether they've been scored.
9. **Results table is summary-only.** Columns are fixed at: **Candidate | Email | Submitted | Overall | Actions**. Per-dimension scores (Skills / Experience / Education), strengths, weaknesses, reasoning, and CV text belong in the **Details modal**, never in the table. This keeps the table narrow enough to fit without horizontal scroll and keeps the Actions column (Details / Shortlist / Reject) fully visible on every row. **Sort order:** unevaluated/new candidates first, then evaluated-pending, then shortlisted, then rejected. Within each group: newest submission first. **Filter bar** above the table with pill buttons: All, **Active (default)**, Shortlisted, Rejected, Duplicates, Archived. Defaults to Active for the same reason Shortlist defaults to Shortlisted — the candidates HR is currently working on are the primary view; rejected/archived are reference data. Each button shows a count badge. **Candidates stat card** shows unique count (total minus duplicates). **Archive flow:** every candidate has an Archive button — pending (newly uploaded, no decision yet), shortlisted, rejected, hired, interviewed, and duplicates. On click: row fades out (0.35s), toast shows "Candidate archived — Undo" for 5s. Undo restores immediately; after 5s the archive commits to `localStorage` (`hr_archived_candidates_v2`, object mapping candidateId → previousStatus, where `previousStatus` is `'pending'` for not-yet-decided candidates). Archived candidates appear in the Archived filter tab with muted styling (greyed rows) and a Restore button. The archived badge varies by previous status: red "✗ Rejected" for rejected, neutral gray "📦 Archived" for pending, green "✓ {Status}" for shortlisted/interviewed/hired. No database records are deleted. **Duplicate detection:** candidates sharing the same email (case-insensitive, trimmed) are grouped. Within each group: the primary is the evaluated candidate (or newest if none evaluated); all others are marked as duplicates. Duplicate rows show a yellow "Duplicate" badge next to the name, amber row tint, and "Archive Duplicate" instead of Shortlist/Reject. A warning banner appears above the table when duplicates exist (hidden when viewing the Duplicates filter). Active filter excludes duplicates by default.
10. **Set Criteria is a 3-row stacked layout.** Row 1 is a 2-column grid (`.criteria-grid`), rows 2–3 are full-width cards below it:
    - **Row 1 left — Criteria Source:** saved-set dropdown (or empty-state message if none exist; default reads "Create new criteria (from scratch)"). No action buttons here.
    - **Row 1 right — Scoring Preferences:** compact weight sliders with running total (must equal 100%). Both cards match height within the row via grid default stretch.
    - **Row 2 — Action Panel** (`.criteria-actions-card`, full width): titled "Choose How to Create Criteria" with four action buttons (From Job Description · Write / Paste · AI Generate · Upload File) and a `.criteria-action-content` area for source-specific controls. "From Job Description" loads the selected job's existing description directly into the criteria draft with one click — this is the fastest path when the JD already describes what's needed. All dynamic content stays inside this card.
    - **Row 3 — Criteria Draft** (`.criteria-draft-section`, full width): textarea that every source populates, plus unsaved-criteria warning and save checkbox.
    **Save logic:** when the save checkbox is checked, the user must provide a criteria set name (prompted on Continue if not already entered); saved sets appear immediately in the dropdown. Responsive: grid stacks to single column under 900px. Never put action buttons inside the Criteria Source card, never let dynamic content float outside the action card, and never add a second "Generated Criteria" textarea.

### Emails (Phase 5)
9. **SMTP status must be real, not "unknown".** Four states: `not_configured`, `configured_not_tested`, `healthy`, `failing`. The Emails page surfaces the live status with a colored badge.
10. **Every send attempt is logged** — `email_log` row with candidate, job, recipient, subject, timestamp, status, and error message if any.
11. **Every candidate-facing email is editable before send.** Subject and body are real `<input>` / `<textarea>` fields, never read-only preview. Default templates prefill on open; a "Reset to default template" button restores them. The backend uses the user-edited subject/body verbatim (via `custom_subject` / `custom_body`) — it never silently regenerates or overwrites user input. This rule applies to **every** email flow: rejection, shortlist notification, interview invitation, job offer, and any future status-change email. All flows route through one shared `openEmailComposer({ candidate, job, emailType, defaultSubject, defaultBody, sendLabel, showSendToggle, onSend, onBack, backLabel })` helper exposed by `EmailComposerModal` — multi-step flows (e.g. interview-pack handoff) pass `onBack` to surface a left-aligned `← Back` button so HR can return to the previous modal without aborting (`frontend-react/src/components/modals/`) — don't build per-flow modals.
12. **Email composer validation before send:** subject non-empty, body non-empty, recipient email contains `@`. The "Reject Candidate" flow is the only one that allows completing the action **without** sending an email (via the "Also send email" toggle); all other flows require a valid recipient.

12b. **Interview Question Generator (InterviewQuestionsModal).** Lives on the **Shortlist tab** as part of the Hand Off to HM flow (see §18). It is **not** exposed from the CV Evaluation detail modal. The modal has **3 source tabs** for building a question list:
- **From Question Bank** — compact searchable list with category chips + checkboxes + "Add X Selected" button. Pulls from `/interview/question-bank`.
- **AI Generate** — count slider (3–20), type checkboxes (Behavioural / Technical / Compensation), optional extra context, "Generate & Add Questions" button. Questions are added **directly** to the unified list — no preview/confirm step. Clicking generate always adds immediately so the questions appear in the email pack.
- **Write My Own** — category dropdown + textarea + "Add" button (Ctrl+Enter shortcut).

All three sources feed one **unified question list** below the source panel. Each question shows a category pill, question text, note textarea, "→ Bank" button (saves that question to the shared bank), and a × remove button. Questions are auto-sorted HR → Technical → Compensation. Below the list: General notes textarea.

**Persistence:** "Save to Profile" footer button saves to both `localStorage` (`hr_interview_notes`, keyed by candidate id) AND `candidate_prepared_questions` DB table via `POST /candidate-questions` (upsert by candidate_id + job_opening_id). This makes questions follow the candidate everywhere. Loads from localStorage on modal open; falls back to DB fetch if no local copy.

**Meeting metadata** block (below source panel): platform pill buttons (Zoom / Teams / Meet / In person / Phone), date+time picker, link/location input (label adapts to platform), interviewers free-text. All optional. Persists with the questions in both localStorage and DB.

**Footer actions:** Close | Save to Profile (green) | Send Pack to HM (blue — always visible, opens EmailComposerModal with the current questions baked into the template body). **Word export** and **Copy** buttons appear in the question list header when questions exist. Logs to `email_log` with `email_type='recommendation'`.

**CRITICAL:** The "→ Bank" per-question button and "Generate & Add" direct-add behavior are intentional — AI-generated questions MUST land in `questions` state (not a separate preview) so they are always included in the email pack when "Send Pack to HM" is clicked.

12a. **Email Recommendation to Hiring Manager.** From the candidate detail modal (CV Evaluation results), HR can send the candidate's evaluation summary to the hiring manager. This is the **only** HM-facing button in the CV Eval detail modal — Generate Interview Questions has been moved to the Shortlist tab's Hand Off flow (see §12b and §18). The "Email Recommendation to Hiring Manager" button appears only for evaluated candidates. Clicking it opens the shared email composer with `recipientLabel: 'Hiring manager'`, `editableRecipient: true` (so HR can type/correct the email inline), and a pre-filled body containing the candidate name, score breakdown (overall/skills/experience/education), strengths, weaknesses, and reasoning. **The To: field is intentionally empty on every open** — HR types the HM address each time. Smart prefill from `job.reporting_to` and the cached `hr_hiring_manager_emails` map was disabled per user request to eliminate any chance of accidentally sending the internal recommendation to the candidate (the composer used to fall back to `candidate.email` when no HM address was known). The cache is **still written** on every send (`hr_hiring_manager_emails`, mapping jobId → email) so re-enabling smart prefill later is a one-line change. Sent emails are logged with `email_type='recommendation'`. The Phase 5 workflow accepts `'recommendation'` as a valid `email_type` (treated as custom — uses `custom_subject`/`custom_body` verbatim).
13. **Email history table rows are expandable.** Each row in the Emails tab is clickable — clicking expands an inline detail panel showing: recipient, type, full date, delivery status badge, error message (if failed), full subject, and complete email body. A dedicated toggle arrow column (last column, fixed 28px width) stays in the same position whether expanded or collapsed so users can toggle without moving the mouse. Only one row can be expanded at a time.

### Job Openings (Phase 2)
18. **Job Detail Modal is fully editable.** Clicking a job title opens a detail modal in view mode. An "Edit" button switches to edit mode with form fields for: Job Title (required), Department (dropdown + custom "Other" input), Employment Type, Seniority Level, Location Type, Reporting To, and Job Description (textarea). "Save Changes" POSTs to `/job-opening-update` with the job ID and changed fields. "Cancel" reverts to view mode without saving. The modal footer switches between view actions (Edit / Activate|Deactivate / Close) and edit actions (Cancel / Save Changes). Department uses a select with predefined options; selecting "Other" reveals a text input for custom department names. All fields except Job Title are optional. **No-op save handling:** if the user clicks "Save Changes" without modifying any field, the modal shows "No changes to save" (info toast), exits edit mode, and closes — it never sends a request or shows an error.
19. **Job toggle patches local state.** Activating/deactivating a job in the table or detail modal patches the local `allJobs` array in-place (`setAllJobs(prev => prev.map(...))`) — never calls `loadJobs()` which would cause a flash/reload of the entire table.

### Shortlist (Phase 4)
11. **Reconsider / status changes refresh only the affected card**, not the whole page. Status transitions are patched in place via `updateShortlistStatus(id, status)`.
12. **Shortlist tab has archive support** with the same UX as CV Evaluation Results: filter bar (All / **Shortlisted (default)** / Interviewed / Handed off / Hired / Rejected / Archived), Archive button on every card, undo toast with 5s window, Restore in archived view. Archived state is stored in `localStorage` under `hr_shortlist_archived` (object mapping shortlist id → previous status). No database records are deleted. The tab defaults to **Shortlisted** (not All) because the active hiring pipeline is the primary view — rejected candidates are reference data, not the daily focus. **Handed off is a derived stage** (no DB column): a candidate is in the Handed-off bucket when at least one `email_log` row with `email_type='recommendation'` and `status='sent'` exists for them and they're not yet hired/rejected. The Shortlisted and Interviewed pills exclude handed-off candidates so the same person never appears in two pipeline stages at once. Hired/Rejected counts are taken from `shortlist.status` and ignore handoff state because they represent terminal verdicts.
13. **Email status messages must be specific.** `sent` = green success with recipient. `logged` = red error "SMTP not configured, saved to log only." `failed` = red error with provider error message. Never show vague "Email logged only" — always explain why and what happened.
14. **Candidate cards show communication status with expandable details.** Each card displays the latest email summary (type, status, timestamp) in a clickable banner. Clicking expands to show full email history for that candidate: all emails sorted newest-first, each with recipient, subject, date, body content, and error message if failed. Data is fetched from `/email-history?job_id=N` in parallel with the shortlist load and stored in `emailMap` as an array per `candidate_id`. After sending, the array updates immediately without full reload. Styling: green banner for sent (`.sl-email-sent`), red for failed/logged (`.sl-email-failed`). Expanded details slide in with animation. Note: "sent" means accepted by the SMTP server — actual delivery/read tracking requires a transactional email service (SendGrid, Mailgun, etc.).
15. **Candidate cards have full visual state transitions.** Each card receives a CSS state class (`candidate-card--shortlisted`, `candidate-card--interviewed`, `candidate-card--hired`, `candidate-card--rejected`) that tints the entire card background and border. On state change: card plays a scale+sweep animation (`candidate-card--transitioning`), a status chip appears in the top-right corner (`.card-status-chip`), action buttons are replaced with a state badge (`.card-state-badge` with slide-in animation) for decided statuses (hired/rejected). Cards stay visible in the current filter via `retainedInView` (cleared on filter switch) so the user sees the visual result before the card moves to its proper filter. **Toast styling:** reject = red (`error`), hired = green (`success`), interviewed = blue (`info`). The card itself is the primary feedback — toasts are secondary.
16. **Notified vs not-notified visual distinction.** Shortlisted candidates who have had at least one email successfully sent (`status === 'sent'`) get an upgraded card style: stronger green tint, green left accent border (`candidate-card--notified`), and a "✉ Notified" chip next to their name. Candidates without a sent email keep the lighter default shortlisted styling. This makes it immediately obvious at a glance who has been contacted vs who hasn't.
17. **Shortlist cards are sorted by most recent first.** Cards are sorted by `updated_at` (falling back to `shortlisted_at`), newest first. This ensures freshly acted-on candidates appear at the top.

18. **Hand Off to Hiring Manager (chained flow).** Shortlisted and interviewed cards expose a "Hand Off to HM" button. Click does **not** go straight to an email composer — it opens `InterviewQuestionsModal` first so HR fills meeting details and (optionally) generates interview questions. From inside that modal, "Send Pack to Hiring Manager" then opens the shared `EmailComposerModal`. The pack body is built by `getInterviewPackTemplate()` (in `services/email.js`) and includes: evaluation summary (overall + per-dimension scores + strengths + areas to probe), meeting details block, numbered question list with category prefix and hint sub-line, optional general notes, and an explicit ownership-transfer line. Both the meeting and the questions are optional — the pack will send with just the evaluation summary if HR hasn't filled either. **Recipient is intentionally empty on every open** — HR types the HM address each time (smart prefill from `job.reporting_to` / cached `hr_hiring_manager_emails` was disabled per user request to prevent accidental candidate-leak; the cache is still written on send so re-enabling is a one-line change). Logs to `email_log` with `email_type='recommendation'`. The modal accepts an `onPackSent(candidateId, newEntry)` callback that the parent (`Shortlist.jsx`) uses to patch the local `emailMap` so the card's email banner updates without a reload. The legacy single-shot handoff template (`getCandidateHandoffTemplate`) is kept in `services/email.js` for now in case anything else wants the bare-summary format, but no UI calls it.

19. **Handed off to HM is its own pipeline stage** (derived, no DB column). Once a candidate's `email_log` contains a sent `recommendation` row they are surfaced in the "Handed off" filter on the Shortlist tab and removed from the Shortlisted/Interviewed pills (so the same person doesn't appear in two stages). Visual treatment: blue card tint (`.candidate-card--handed-off`), blue "✉ Handed off to HM" chip in the card header, and the email banner switches to blue (`.sl-email-handoff`) when the latest email is a recommendation. **Action relocation:** Shortlisted and Interviewed cards no longer expose Hire/Reject — those are HM-driven verdicts that only surface once the pack has been sent. Handed off cards show: `Hire` (green) · `Send Offer` (if email on file) · `Reject` (red) · `Re-send Pack` (re-opens InterviewQuestionsModal). Hire/Reject still write to `shortlist.status` so terminal verdicts move the card into the Hired/Rejected pill as before — handoff state is "active processing", not a status replacement. **Email type label:** the user-facing string for `email_type='recommendation'` is "Handed off to HM" everywhere — Shortlist banner, Emails table TYPE column, email history detail. Centralized in `utils/helpers.js#emailTypeLabel(type)` so any future renames touch one spot. The backend type stays `recommendation` (no migration).

21. **Live Interview page has 3 sub-tabs:** Setup, Question Bank, and **Candidate Prep**. The Candidate Prep tab fetches `/candidate-questions-list?job_id=N` and shows all candidates with saved prep questions for that job. Each card shows candidate name, question count, meeting details summary, and last-saved date. Clicking the card expands it to show all questions. **"Use for Interview"** button switches to the Setup tab, triggers `handleJobChange` for the candidate's job, and queues the candidate + questions via `pendingPrep` state — once `candidates` loads (detected via `useEffect`), the candidate is auto-selected and the prepared questions are loaded into the AI-generate slot (`setGeneratedQs`, `setQMode('ai-generate')`). The **Setup tab** question section also has 3 source cards (From Question Bank / AI Generate / Write My Own) matching the InterviewQuestionsModal's source tabs for visual consistency.

20. **Inbound replies are first-class email_log rows** (direction='inbound'). `scripts/imap_server.py` polls the configured Gmail mailbox (UNSEEN, every `IMAP_POLL_SEC`, default 60s) and POSTs each parsed message to the n8n `/inbound-email` webhook. The webhook (a) validates `from`, (b) drops cold inbounds with no `In-Reply-To` / `References` headers (they have no parent to attach to and our `email_log` schema requires `candidate_id` + `job_opening_id`), (c) looks up the parent outbound row via `email_log.message_id = $in_reply_to` (or the last token of `References` as fallback), (d) inserts an `email_log` row with `direction='inbound'`, `email_type='custom'`, `status='sent'`, the parent's `candidate_id`/`job_opening_id`, the inbound `subject`/`body`/`message_id`/`in_reply_to`, and the **sender** address in `recipient_email` (column reused as "the other party" — see migration 009 comment). Orphan replies (no matching parent row) get a 200 response with `{orphan: true, reason}` so the IMAP sidecar still marks them Seen and stops re-forwarding on every poll. **Frontend rendering rule:** inbound rows must be visually distinct from outbound rows everywhere they appear. Shortlist card banner: purple tint (`.sl-email-inbound`) + 📥 icon + "Reply from {sender}" instead of the email-type label. Detail panel: purple-tinted item (`.sl-detail-inbound`), "Reply received" header, badge says "📥 Inbound", "From:" label instead of "To:". Emails table: purple row background, dedicated "Reply" type pill, "inbound" status pill, "from" prefix in the recipient cell, dedicated "Inbound" filter tab + stat card, and `sentCount`/`failedCount` exclude inbound rows (they only count outbound delivery). The `isHandedOff()` derivation in `Shortlist.jsx` and the `hasEmailSent` "Notified" chip both filter out inbound rows so receiving a reply doesn't accidentally toggle pipeline state. **SMTP outbound dependency:** the SMTP sidecar generates a per-send Message-ID (`scripts/smtp_server.py#make_msgid`) and returns it in the JSON response; the Phase 5 `Send - Log Result` query persists it on the outbound `email_log` row so the threading lookup above works. Without that Message-ID round-trip, every inbound reply lands as an orphan.

---

## 5. State Model

### 5a. Global Selected Job (cross-tab context)

Every tab that operates on a single job (CV Evaluation, Shortlist, Emails, and optionally Dashboard) reads from the **same** job selection. This is stored in one place — `globalSelectedJob` — and mirrored into each tab's per-tab selector. A "Current Job" badge in the header always shows the active job so the user never loses context when switching tabs.

**State:**
```js
let globalSelectedJob = null;   // { id, job_title, department } or null
const GLOBAL_JOB_KEY = 'hr_selected_job';   // localStorage key
```

**Lifecycle rules:**
1. **Single source of truth.** Any job selection (CV Eval card click, Shortlist dropdown, Email filter, Dashboard filter with a specific job) calls `setGlobalSelectedJob(job)`, which updates the in-memory state, writes to `localStorage`, re-renders the header badge, and mirrors the value into every other tab's selector via `syncSelectorsToGlobal()`.
2. **Persistence.** `loadGlobalSelectedJob()` runs once at init (before `loadDashboard`) to restore the last-selected job from `localStorage` so selection survives reloads.
3. **Per-tab population.** `loadJobsForShortlist()`, `loadJobsForEmailFilter()`, and the Dashboard's filter read `select.value || globalSelectedJob.id` when rebuilding options — so the first visit to any tab preselects the global job and immediately loads its data.
4. **CV Evaluation entry.** On the Select Job step, if no card is selected yet and a `globalSelectedJob` exists, `loadJobsForEval()` auto-clicks the matching card. If the user is already mid-wizard on a different job (past Step 1) we do **not** switch jobs on them — per-tab override wins for workflow continuity.
5. **Dashboard "All Jobs" exception.** On the Dashboard, picking a specific job updates global as usual, but switching to "All Jobs" is a dashboard-local view and does NOT clear the global selection — other tabs keep their current job.
6. **Clearing.** The `&times;` button on the header badge calls `clearGlobalSelectedJob()` which removes localStorage and resets every mirrored selector to empty.
7. **Option data attributes.** Every `<option>` populated for job selectors carries `data-title` and `data-dept` so `readJobFromSelect()` can reconstruct `{id, job_title, department}` without an extra API lookup.

Selector IDs that mirror `globalSelectedJob`:

| Tab | Selector | onchange handler |
|-----|----------|------------------|
| Dashboard | `#dash-job-filter` | `onDashboardJobFilterChange()` |
| CV Evaluation | (job cards, not a `<select>`) | `selectJobCard(id)` |
| Shortlist | `#shortlist-job-select` | `onShortlistJobChange()` |
| Emails | `#email-job-filter` | `onEmailJobFilterChange()` |

### 5b. CV Evaluation Wizard

The Phase 3 wizard stepper is driven by three booleans per job, derived from `fetchJobState(jobId)`:

```
has_criteria     = /criteria-sets?job_id=N   returns ≥ 1 row
has_cvs          = /candidates?job_id=N      returns ≥ 1 row
has_evaluations  = /evaluations?job_id=N     returns ≥ 1 row
```

**Access rules** (enforced by `evalStepClick`):
- Step 1 (Select Job): always
- Step 2 (Set Criteria): if `job_selected`
- Step 3 (Upload CVs): if `job_selected`
- Step 4 (Results): if `job_selected && (has_cvs || has_evaluations)`

**Edge case:** `has_evaluations && !has_criteria` means evaluations were scored with ad-hoc criteria text that was never saved to `criteria_sets`. The job status badge shows "Criteria used (not saved)" in yellow instead of "No criteria" to avoid confusion.

---

## 6. Candidate Status Model

Status is stored in `shortlist.status`. Values (via DB CHECK constraint):

| Value | Meaning |
|-------|---------|
| `shortlisted` | HR moved them forward from evaluation results |
| `interviewed` | Interview completed |
| `hired` | Offer accepted |
| `rejected` | Declined (can be triggered from eval results or from shortlist) |

A candidate without a `shortlist` row is effectively **pending** — scored but no decision made.

Rejection also triggers a row in `email_log` with `email_type = 'rejection'`.

---

## 7. Email Rules

### SMTP States
- `not_configured` — `SMTP_HOST` env var empty. Emails get logged with status `logged`, not actually sent.
- `configured_not_tested` — SMTP env vars set, but no recent sends to confirm they work.
- `healthy` — recent sends succeeded.
- `failing` — recent sends failed (bad credentials, server down, etc.).

### Test Connection / Test Email
- The sidecar `GET /` returns whether `SMTP_HOST` is set.
- Planned: `POST /test-connection` (dry-run SMTP handshake) and `POST /send-test` (real send to an address HR enters).

### Logging Requirements
Every send attempt, success or failure, must insert into `email_log` with:
- `candidate_id`, `job_opening_id`
- `email_type` (`rejection` / `interview_invite` / `offer` / `custom`)
- `recipient_email`, `subject`, `body`
- `status` (`sent` / `failed` / `logged` / `pending`)
- `error_message` (only set on failure)
- `sent_at` (or attempted_at)

---

## 8. Fix Log

Major bugs + how they were fixed. Use this to avoid re-introducing them.

| Date | Bug | Root cause | Fix |
|------|-----|------------|-----|
| 2026-04 | "Run Evaluation" button sat in Upload CVs step | Mixed responsibilities | Split — Upload is upload-only, Results step has the "Run Evaluation" button |
| 2026-04 | Shortlist "Reconsider" reloaded entire page | Called `loadShortlist()` on status change | In-place patch via `updateShortlistStatus(id, status)` |
| 2026-04 | Reject button clipped / missing on some rows | `display:flex` on `<td>` is invalid + `.table-wrap` had `overflow:hidden` | Wrapped buttons in `<div class="actions-container">` inside `<td class="actions-cell">`, added `.table-scroll { overflow-x: auto }`, made Reject candidate-level |
| 2026-04 | Criteria auto-generated without user asking | Various triggers (load, job select, tab switch) all fired generation | Audited all code paths — only the "Generate Criteria" button `onclick` triggers generation |
| 2026-04 | Generate button appeared before weights | Originally placed before the weight sliders | Moved after weights so it reads the current values |
| 2026-04 | Set Criteria blocked for existing jobs | `evalStepClick` required `.completed` class on target step | Rewrote `evalStepClick` — Step 2 is reachable whenever a job is selected |
| 2026-04 | "No criteria" shown despite evaluations existing | `has_criteria` only checks `criteria_sets` table, but evals can run with ad-hoc text | Added "Criteria used (not saved)" yellow pill for the `has_evaluations && !has_criteria` case |
| 2026-03 | n8n webhooks didn't register after import | `activeVersionId` was not set in `workflow_entity` | `UPDATE workflow_entity SET active=1, activeVersionId=versionId WHERE id='N'` in sqlite after every import |
| 2026-04 | Patching `workflow_entity.nodes` directly in sqlite did not change runtime behavior — n8n kept executing the old version of Code/HTTP nodes even after a full restart | n8n executes the **snapshot** stored in `workflow_history.nodes` indexed by `workflow_entity.activeVersionId`, not the editor draft in `workflow_entity.nodes`. Updating only the entity row leaves the runtime unchanged | Any sqlite-level workflow patch must update **both** `workflow_entity.nodes` AND the matching `workflow_history` row (`UPDATE workflow_history SET nodes = ? WHERE versionId = (SELECT activeVersionId FROM workflow_entity WHERE id = ?)`). `scripts/patch_ollama_thinking.py` is the canonical example. After updating, kill all `node` processes whose command line contains `n8n` (this includes the separate `@n8n/task-runner` worker) and restart. The legacy "import → set activeVersionId" path is unaffected because import recreates the history row from the entity row |
| 2026-06 | All patch scripts (`patch_ollama_thinking.py`, `patch_imap_inbox.py`, `patch_eval_today.py`) were writing to `C:\Users\Jasse\.n8n\database.sqlite` (Windows USERPROFILE default) instead of the actual running DB at `D:\n8n\.n8n\database.sqlite` (set by `N8N_USER_FOLDER=/d/n8n` in `start.sh`). Every patch since project start silently wrote to the wrong file — new nodes (interview questions, remove-from-shortlist, inbound-email, cv-file) never reached the live instance | `N8N_DB` was computed as `Path(os.environ["USERPROFILE"]) / ".n8n" / "database.sqlite"` which resolves to C drive regardless of where n8n actually runs | Fixed all three scripts to hardcode `Path("D:/n8n/.n8n/database.sqlite")`. Manually patched workflows 2, 3, 4 to the D drive DB and re-activated them via REST API. **Rule:** n8n DB is always `D:\n8n\.n8n\database.sqlite`. Never derive it from USERPROFILE or HOME. Calling `/deactivate` via REST also clears `activeVersionId` — always set `active=1, activeVersionId=<ver>` in the DB directly before calling `/activate` |
| 2026-04 | Ollama JD generation leaked reasoning into stored job descriptions — JDs began with "Okay, the user wants me to create..." or contained `</think>` tags. Earlier `think:false` flag suppressed the explicit `<think>` block but qwen3:4b still emits inline preamble ("Okay…", "Let me…", "I notice…") in the `response` field | (a) `think:false` only stops the structured thinking field; the model still narrates before producing the JD. (b) The phase1 prompt didn't forbid preamble or anchor the first line. (c) The parser took `response` verbatim with no preamble strip. (d) Earlier patcher only touched `workflow_entity.nodes` so the runtime kept the old parser (see row above) | New `scripts/patch_ollama_thinking.py` does three things idempotently: (1) Rewrites the phase1 `Create - Ollama - Generate JD` HTTP body — strict output rules, lower temperature (0.3), explicit "first line MUST be `## About the Role`". (2) Wraps the `Create - Process AI Response` (phase1) and `GenCrit - Parse Response` (phase2) parsers with a shared `stripLLMPreamble()` JS helper that strips wrapping `<think>` blocks (matched + unmatched + stray tags + markdown fences), seeks to the first valid heading (`## About the Role`, `Job Title:`, `**Field:`, etc.), and falls back to dropping leading reasoning lines via regex. (3) Patches both `workflow_entity` and `workflow_history` in the live sqlite. Verified end-to-end: fresh JD generation now starts with `## About the Role` directly, `</think>` and "Okay…" markers absent. Also patched in source JSONs so re-import preserves the fix |
| 2026-04 | Results table overflowed horizontally — Actions column clipped behind scrollbar | Table mixed summary + detail columns (Skills / Experience / Education badges) | Simplified to 5 summary columns (Candidate / Email / Submitted / Overall / Actions). Per-dimension breakdown moved exclusively to the Details modal. Removed `.table-scroll` wrapper since the narrower table fits natively |
| 2026-04 | Candidate emails not editable before send — subject locked, body preview-only | Reject modal used `<span>`/`<pre>` read-only elements; shortlist flow passed `custom_*` only when `email_type === 'custom'` | Replaced modal with editable `<input>` + `<textarea>`. All flows now route through a shared `openEmailComposer()` helper. Every send passes `custom_subject`/`custom_body` verbatim to `/send-email`. Added validation (non-empty subject/body, valid email) and a "Reset to default template" button |
| 2026-04 | Selecting a job in one tab didn't carry over to other tabs — user had to reselect on every tab | Each tab had its own state (`evalSelectedJob`, `#shortlist-job-select`, `#email-job-filter`, `#dash-job-filter`) with no shared source of truth | Introduced `globalSelectedJob` (module-level + `localStorage` under `hr_selected_job`). Every per-tab selector mirrors it via `syncSelectorsToGlobal()`, every selection calls `setGlobalSelectedJob()`. Header renders a "Current Job" badge. CV Eval Step 1 auto-selects the matching card on entry. Dashboard "All Jobs" doesn't clear global (view-only mode) |
| 2026-04 | Set Criteria page was a long vertical stack — saved sets → tabs → manual textarea → AI block with its own textarea → upload block → giant weights → generate block → save checkbox → footer. Users couldn't tell what order to do things in, weights dominated the page, and two textareas (`#criteria-text` + `#criteria-text-ai`) fought for the "real" criteria | Grew organically across three features (manual, AI, upload) with each adding its own panel | Rebuilt as a 3-section, 2-column layout: left = (A) Source + source-specific inputs + (B) compact weight sliders + (C) optional AI Generate block; right = single **Criteria Draft** textarea + unsaved-criteria warning + save checkbox. Removed `#criteria-text-ai` and `syncAICriteriaText()` — generation now writes to the one draft. Added `onCriteriaDraftInput` / `onSaveCriteriaToggle` / `updateUnsavedWarning` helpers. Responsive below 900px |
| 2026-04 | "Use custom criteria" wording was vague; AI Generate mode pushed Criteria Draft box downward; no empty state for saved criteria; save didn't prompt for name | AI Generate block rendered as a separate section between left column and right column, causing layout shift. Dropdown always showed even when no saved sets existed | Renamed to "Create new criteria (from scratch)". Added empty-state message when no saved sets. Moved AI Generate button into source card. Save logic prompts for name if not provided. Saved sets refresh after save |
| 2026-04 | Set Criteria draft squeezed on the right next to config panels — cramped editor, awkward layout | Two-column layout put config (left) beside draft (right), giving the editor less than half the width | Restructured to two-row layout: Row 1 = Criteria Source + Scoring Preferences side by side; Row 2 = full-width Criteria Draft below. Draft now has full content width. CSS class changed from `.criteria-grid`/`.criteria-col` to `.criteria-top-row` |
| 2026-04 | Rejecting or shortlisting a candidate only showed a toast — row actions didn't change, status wasn't visible after refresh | No shortlist status was fetched on Step 4 load; actions were always the same pending set regardless of actual status | Step 4 now fetches `/shortlist?job_id=N` and builds a `shortlistMap`. Rows render state-dependent actions: pending shows buttons, shortlisted shows centered green badge, rejected shows centered red badge. Status updates in-place with pop animation (`statusPop` keyframe). Rows get tinted backgrounds. State persists across refresh |
| 2026-04 | "Run Evaluation" showed generic "Evaluation failed" with no useful info; also failed silently when all candidates were already evaluated (n8n returned empty body) | `apiPost` crashed on empty response body (`res.json()` on empty string); `runEvaluation` catch block had no detail; no pre-check for unevaluated candidates | Fixed `apiPost` to handle empty/malformed response bodies. `runEvaluation` pre-checks candidate state: blocks with clear message if no candidates or all evaluated. Button shows unevaluated count and disables with "\u2713 All Evaluated" when done. Error messages are actionable (network, Ollama, backend, HTTP status) |
| 2026-04 | Shortlist tab state changes felt weak — only a small toast, card barely changed, no visual confirmation of decision | No card-level styling for status, no animation, actions stayed the same after decision | Full card state transitions: CSS state classes tint entire card (green/blue/red), scale+sweep animation on change, status chip in top-right, state badges replace action buttons for decided statuses, `retainedInView` keeps card visible until filter switch |
| 2026-04 | Shortlisted candidates with email sent looked identical to those without email | No visual distinction between notified and not-yet-notified candidates | Added `candidate-card--notified` class (stronger green, left accent border) and "✉ Notified" chip for candidates with at least one successfully sent email |
| 2026-04 | Shortlist tab showed candidates in arbitrary API order — recent actions appeared at bottom | No sorting applied to shortlist data | Added sort by `updated_at` (fallback `shortlisted_at`), newest first |
| 2026-04 | Emails tab showed only summary columns — no way to see email body or full details | Table rows were static, no expand/detail view | Made rows clickable with expandable inline detail panel (recipient, type, date, status badge, error, subject, full body). Toggle arrow in fixed-width last column for stable click target |
| 2026-04 | Shortlist email status only showed latest email with no way to see details or history | `emailMap` stored single object per candidate, no body/recipient data | Changed `emailMap` to array of all emails per candidate, stored body/recipient/error. Added expandable detail panel on click with full email history |
| 2026-04 | Job Openings toggle (activate/deactivate) caused ugly flash — entire table reloaded | `toggleJob()` called `loadJobs()` which re-fetched all jobs and re-rendered the full table | Patched local `allJobs` state in-place with `setAllJobs(prev => prev.map(...))` — no API reload, smooth toggle |
| 2026-04 | Job Detail Modal was read-only — no way to edit job title, department, description, or any field | Modal only displayed data, had no edit mode or update endpoint | Added full edit mode with view/edit toggle. Edit form covers all fields (title, department, employment type, seniority, location, reporting to, description). New `/job-opening-update` POST endpoint added to n8n workflow with dynamic SQL builder. Department supports custom values via "Other" option |
| 2026-04 | Dashboard doughnut chart didn't render — blank space where chart should be | Manual Chart.js with `useRef` + `useEffect` had timing issue — canvas wasn't in DOM when effect fired due to conditional rendering | Replaced with `react-chartjs-2` `<Doughnut>` component which handles lifecycle automatically |
| 2026-04 | CV evaluations produced garbage scores (e.g. flat 5/5/5 or 1.9 overall) — parser fallback kicked in because Ollama's `response` field was empty | qwen3:4b is a thinking model: with thinking enabled, it burns the `num_predict` budget on chain-of-thought in the `thinking` field and returns an empty `response`. The existing parser's lazy regex couldn't extract JSON from the partial thinking output | CV eval Ollama call now passes `think: false` + `format: 'json'` (forces clean, parseable JSON); `num_predict` reduced to 4000, `temperature` to 0.2. Parser rewritten: strips `<think>` tags + markdown fences, brace-matches for JSON extraction, clamps scores to 0–10, sets `parse_error: true` + 0/0/0 + explicit reasoning when structured scores are missing (so HR sees a real failure, not silent fallback). Same `think: false` applied to JD generation (phase1) and criteria generation (phase2) |
| 2026-04 | Every job on the Job Openings page showed a yellow "draft" badge forever, even after toggling active/inactive. Toggling stored invalid status values (`'active'`/`'inactive'`) that would disappear on reload | `prepare-insert` defaulted `status` to `'draft'`, the toggle workflow only flipped `is_active` without touching `status`, and the UI's optimistic update used enum values not in the DB CHECK constraint (`'active'`/`'inactive'` — valid values are `draft`/`open`/`closed`/`on_hold`). Status was effectively orphaned — always `'draft'` from insert, never transitioned | Jobs now default to `status='open'` on creation (they're immediately usable, there's no save-as-draft flow). Toggle workflow updates both columns atomically: `UPDATE job_openings SET is_active = NOT is_active, status = CASE WHEN NOT is_active THEN 'open' ELSE 'closed' END`. Frontend optimistic update uses `'open'`/`'closed'`. Backfilled existing rows: `UPDATE job_openings SET status='open' WHERE status='draft' AND is_active=true` |
| 2026-04 | Shortlisting a candidate was one-way — once added, the only reversal was to reject or archive, both of which pollute the history. No way to "undo" a shortlist decision | Phase 3 workflow only had add/update-status — no delete endpoint | Added `POST /remove-from-shortlist` endpoint (accepts `{id}` or `{candidate_id, job_opening_id}`, runs `DELETE FROM shortlist ... RETURNING *`). Added `unshortlistCandidate()` + "Unshortlist" button to CV Evaluation results row. Button only appears for `status === 'shortlisted'` — interviewed/hired are later decisions that shouldn't be trivially undone from here. Confirms before deletion. On success, candidate returns to pending state and can be re-shortlisted or rejected |
| 2026-04 | Criteria were a single free-text blob — HR couldn't mark items as required vs optional, and had no way to set per-item weights so the AI knew what mattered most | `criteria_sets` only had a single `criteria_text` column + 3 dimension weights. Eval prompt received one unstructured chunk | Added migration `006-criteria-items.sql` with new `criteria_items JSONB` column (default `[]`, backward-compatible). Updated `/criteria-sets` POST to persist items, `/cv-evaluate` to forward them, `Eval - Prepare Prompts` to include an ITEMIZED CRITERIA block listing each item as `[REQUIRED]`/`[optional]` with its weight, and parser now captures `required_missing`/`items_met` arrays from Ollama and appends them to the stored `reasoning`. Frontend adds an editable items panel below the Criteria Draft with per-row text + Required toggle + weight input, showing required/optional/total-weight summary |
| 2026-04 | HR had no way to view the original PDF that a candidate uploaded — only the extracted plain-text CV was available, and PDFs that parsed poorly (multi-column, scanned) showed garbled text with no fallback | `candidates` table only stored `cv_text`; the binary PDF was discarded after `pdfjs-dist` extracted text in the browser | Added migration `007-cv-file-storage.sql` with `cv_file_name`, `cv_file_data` (base64 TEXT), `cv_file_mime` columns. Frontend reads file via `FileReader.readAsDataURL` and sends b64 in `/cv-submit`. Phase 3 workflow stores it in the INSERT and adds a new `GET /cv-file?candidate_id=N` endpoint that returns `{cv_file_name, cv_file_data, cv_file_mime}`. `GET /candidates` includes a derived `cv_file_available` boolean. `EvalDetailModal` shows a "View Original PDF" button next to the CV text section when available; click decodes b64 client-side and opens the blob via `URL.createObjectURL` in a new tab |
| 2026-04 | Pending (newly uploaded, undecided) candidates couldn't be archived — Archive only appeared for finalized (shortlisted/rejected) and duplicate rows. HR had to first reject a candidate to remove them from view, polluting the rejection log | `archiveCandidate()` was wired up but the Archive button was only rendered in the rejected/shortlisted/duplicate branches | Added Archive button alongside Shortlist/Reject in the pending non-duplicate row branch. `archiveCandidate(id)` already defaulted `previousStatus` to `'pending'` so no logic change needed. Archived row badge logic now distinguishes pending (`📦 Archived`, neutral gray) from rejected (red) and shortlisted/interviewed/hired (green). New `.status-archived` CSS class added |
| 2026-04 | HR had no AI-driven way to prepare interview questions for a candidate — they were writing them from scratch each time, and there was no place to record interview notes against the questions actually asked | No interview-question generation existed; CV-eval modal had only score breakdown + email recommendation | Added Phase 3 endpoint `POST /generate-interview-questions` (12 nodes: Webhook → Validate → Valid? → Fetch Candidate & Job → Build Prompt → Found? → Call Ollama → Parse Response → Success/Error/NotFound/Down responders). Validate clamps `num_questions` 3–20, requires at least one of HR/technical/salary, builds the SQL JOIN. Build Prompt assembles a prompt that includes the candidate name, CV (first 6000 chars), job title + JD (first 3000 chars), and the requested categories — calls qwen3:4b with `think:false` + `format:'json'` (same hardened pattern as CV-eval). Parse strips `<think>` tags + markdown fences, brace-matches JSON, then **drops categories that weren't requested** (LLM tends to add salary questions even when excluded). New `InterviewQuestionsModal.jsx` exposes the flow from `EvalDetailModal`: count slider, type checkboxes, optional context, generated list with category-coloured pills + per-question note textarea + general-notes textarea. Notes persist in `localStorage` (`hr_interview_notes`, keyed by candidate id). Word export builds an `application/msword` Blob (downloads `.doc` openable in Microsoft Word) and Copy emits plain text. SQL fix during integration: column was `j.job_description`, not `j.description` |
| 2026-04 | HR couldn't email an evaluation recommendation to the hiring manager — every email flow used the candidate's own address as the recipient | `EmailComposerModal` hardcoded `cfg.candidate.email` for both the visible "To:" and the actual send. Phase 5 workflow's `validTypes` array didn't include `'recommendation'`, and the `email_log` CHECK constraint only allowed `rejection / interview_invite / offer / custom` | Added migration `008-recommendation-email-type.sql` (CHECK constraint now allows `'recommendation'`). Added `'recommendation'` to the Phase 5 workflow's `validTypes` (falls through to the custom branch — uses `custom_subject` / `custom_body` verbatim). Added `getRecommendationTemplate(...)` helper in `services/email.js` that builds a pre-filled body from the evaluation (overall/skills/experience/education + strengths + weaknesses + reasoning). `EmailComposerModal` now supports `cfg.recipientEmail` / `cfg.recipientName` / `cfg.recipientLabel` / `cfg.editableRecipient` so the same modal handles candidate emails AND hiring-manager emails — when `editableRecipient` is true, the To field becomes a typed input. Added "Email Recommendation to Hiring Manager" button to `EvalDetailModal` (only when the candidate has been evaluated). Cached hiring-manager emails per job in `localStorage` (`hr_hiring_manager_emails`) so HR doesn't retype on every send |
| 2026-04 | HR could send a recommendation or interview pack from CV Evaluation, but once a candidate moved into the Shortlist tab there was no way to hand them off to the hiring manager — only candidate-facing emails (shortlist notification, interview invite, offer) were available. HR had to bounce back to CV Evaluation to email the HM, which is awkward once status moves to interviewed | `Shortlist.jsx` only exposed `sendEmail()` flows that hardcoded the candidate's address; no per-card HM action existed | Added `getCandidateHandoffTemplate(...)` to `services/email.js` (subject "Handing off X — Y", body includes candidate contact, full evaluation summary, strengths/weaknesses/reasoning, and an explicit ownership-transfer line). Added `handOffToHM(s)` to `Shortlist.jsx` that opens the shared `EmailComposerModal` with HM recipient pre-filled (from `job.reporting_to` or cached `hr_hiring_manager_emails`). New "Hand Off to HM" button on shortlisted and interviewed cards. On send, caches the HM email per job and patches the result into the local `emailMap` so the card's email banner updates immediately. Logs to `email_log` as `email_type='recommendation'` (no new email type or migration) |
| 2026-04 | Interview prep was scattered — the modal generated questions but had no place to record the meeting platform, time, link, or interviewer names; HR had to put that info in an external calendar tool. There was also no one-click way to send the prepared pack to the hiring manager | `InterviewQuestionsModal` only persisted `{questions, notes, generalNotes}` and exposed only Copy/Word export/Save Notes; meeting data was a TODO. Word export had no meeting section. No template existed for "interview pack" emails | Added a "Meeting details" block to `InterviewQuestionsModal` with platform dropdown (Zoom / Microsoft Teams / Google Meet / In person / Phone), datetime-local picker, link/location input (label adapts to platform), and interviewer free-text. Extended the localStorage payload to `{...prev, meeting:{platform, datetime, link, interviewers}}`. The link field repurposes for office address when "In person" or for phone number when "Phone" is selected. Added `getInterviewPackTemplate(...)` to `services/email.js` (numbered question list with category prefix + hint sub-line + meeting block + general notes). Added "Send Pack to Hiring Manager" button that opens the shared `EmailComposerModal` with recipient pre-filled from `job.reporting_to` or cached `hr_hiring_manager_emails`. Logs to `email_log` with `email_type='recommendation'` (re-uses the existing relaxed CHECK constraint — no new email type needed). Word export now includes a "Meeting" table at the top when any meeting field is set. **Not implemented:** no actual Zoom/Teams API integration — the platform dropdown is metadata only, HR creates the calendar invite manually |
| 2026-04-27 | "View Original PDF" / "View CV" silently did nothing — Chrome's popup blocker killed `window.open(blobUrl, '_blank')` because it fired *after* the async `/cv-file` fetch resolved. By that point the user-gesture chain that authorizes popups had expired, so the call was treated as programmatic and dropped without any console error | The handler called the n8n webhook first, then opened a tab with the resulting blob URL. Modern browsers only honor `window.open` when invoked synchronously inside the click handler. The fallback `<a download>` path silently triggered a download (also confusing, because the file appeared in the downloads bar instead of opening) | `viewCV` (CVEvaluation.jsx) and `viewOriginalCV` (EvalDetailModal.jsx) now call `window.open('about:blank', '_blank')` synchronously on click, then `win.location.href = blobUrl` once the fetch resolves. Refactored `pdf.js` to expose `base64ToBlobUrl(b64, mime)` — `openBase64InNewTab` is kept for backwards compat but the async-flow callers should use the synchronous `window.open` + `base64ToBlobUrl` pattern. On error the prelaunched `about:blank` tab is closed via `win.close()` so the user doesn't get a dangling blank tab |
| 2026-04-27 | Even after the popup-blocker fix above, "View Original PDF" still showed "Original CV file is not available" — the prelaunched tab opened, then closed itself with the toast, despite the n8n endpoint returning a fully populated `cv_file_data` blob | `viewOriginalCV` in `EvalDetailModal.jsx` was double-unwrapping the response. `services/api.js#apiGet` returns the parsed body directly (`res.json()` — **no** `{data: parsed}` wrapper, unlike `apiPost`). So `res` was already `{success: true, data: {cv_file_data, ...}}` — the code did `const data = res.data` (correctly grabbing the inner object) and then checked `!data.data || !data.data.cv_file_data` (looking for a second-level `.data` that doesn't exist), which always failed. The `viewCV` sibling in `CVEvaluation.jsx` got it right with `res?.data?.data || res?.data` | Mirrored the safer accessor: `const d = res?.data?.data || res?.data || {}` and `if (res?.success === false || !d.cv_file_data)`. Tolerates either `apiGet` returning the body directly OR (future) being changed to wrap in `{data}`. Added a comment in the file explaining the `apiGet` vs `apiPost` shape mismatch so the next person doesn't repeat this. **Reminder for future endpoints:** `apiGet` ≠ `apiPost` shape — `apiGet` is "raw body", `apiPost` is `{status, data}`. If you find yourself writing `res.data.data.X` after an `apiGet`, you've nested one level too many |
| 2026-04-27 | When HR opened the "Hand Off to HM" / "Email Recommendation to Hiring Manager" composer with no HM address known, the To: field showed the **candidate's** email by default — bizarre and dangerous since one accidental Send would have leaked the internal evaluation summary to the candidate themselves | `EmailComposerModal` had `const defaultRecipient = cfg.recipientEmail \|\| cfg.candidate.email \|\| '';`. When `cfg.recipientEmail` was empty (no cached HM, no `job.reporting_to` email), it fell through to `cfg.candidate.email`. The candidate-fallback was correct for candidate-facing flows (rejection / shortlist / interview invite / offer) but completely wrong for HM-facing flows | Two-part fix. (1) `EmailComposerModal` now branches on `cfg.editableRecipient`: when true (HM-facing), the candidate fallback is bypassed and the field starts empty (or with whatever `cfg.recipientEmail` was passed); when false (candidate-facing), the old fallback chain stays. (2) Both HM callers (`InterviewQuestionsModal#sendPackToHM` and `EvalDetailModal#emailRecommendation`) now pass `recipientEmail: ''` — per user request the To: field is **completely empty** on every open, even when a stored cache or `job.reporting_to` exists. Cache is still **written** on send (`saveHMEmail`) so re-enabling smart prefill later is a one-line change in each caller |
| 2026-04-27 | The Platform field in the interview-prep meeting block used a native `<select>` — on Windows that pops the OS dropdown, which has no styling, opens with an unfriendly system animation, and looks out of place next to the rest of the modal's design. With only 5 fixed platforms there's no scaling reason to use a dropdown at all | Native selects are unstyleable for the open/close behavior — `appearance: none` only affects the closed state. The system menu always wins | Replaced the `<select>` with a row of pill buttons (`role="radio"` inside a `role="radiogroup"` for accessibility). Each pill carries an emoji icon + short label (Zoom / Teams / Meet / In person / Phone). Selected state: filled blue background, white text, soft shadow. Unselected: white with gray border. Hover lightens the background; mousedown scales 0.97 for tactile feedback. All transitions are CSS (`background-color 160ms ease`, `transform 120ms ease`) so no jank. Click an already-selected pill to deselect (matches the empty-string initial state). Stored value is unchanged (`'Zoom' / 'Microsoft Teams' / 'Google Meet' / 'In person' / 'Phone'`) so the existing `meeting.platform === 'In person'` conditionals for the link/location label still work, and the email pack template + Word export need no changes |
| 2026-04-27 | Generated interview questions came out in arbitrary order — qwen3:4b interleaved HR / technical / salary types instead of grouping them. The Word export, copy output, and HM email pack all inherited that random ordering, so the recipient saw e.g. `[HR] Q1, [TECHNICAL] Q2, [HR] Q3, [SALARY] Q4, [TECHNICAL] Q5...` instead of a clean flow | Sort-on-display was never wired in — the modal trusted the LLM's order. The pack template, export, and copy pulled from that same unsorted state | Added `CATEGORY_ORDER = { hr: 0, technical: 1, salary: 2 }` and a `sortQuestionsByCategory(qs, notesArr)` helper in `InterviewQuestionsModal.jsx` that returns paired-and-sorted `{questions, notes}` so per-question notes stay aligned. Applied at both entry points: fresh `generate()` (sort the LLM output before `setQuestions`) and the `useEffect` localStorage load (sort previously-saved sets too — paired sorting preserves any notes HR had already attached). Categories outside the known set get `?? 99` so unexpected types fall to the end without crashing. All downstream consumers (display, Word, copy, pack template) see the same sorted order automatically — no changes needed there |
| 2026-04-27 | After the chained Hand Off → InterviewQuestionsModal → EmailComposerModal flow shipped, HR had no way to step **back** from the composer to tweak the meeting time / regenerate questions / edit interviewer list before sending. Cancel just closed the composer (which technically did reveal the questions modal underneath, since it was still mounted), but it wasn't discoverable — users assumed Cancel = abort whole flow | The `EmailComposerModal` only had Cancel + Send. No way for the caller to signal "I'm a step in a multi-step flow, give me an explicit Back affordance". Local subject/body state also leaked across opens — if you canceled, edited the meeting, then reopened the composer, the previous edits would override the freshly-built template | Added an optional `onBack` callback (and matching `backLabel`) to the composer cfg. When present, the footer renders a left-aligned `← {backLabel}` button before Cancel/Send, and clicking it runs `onBack()` then closes the composer. Refactored close handling into a single `handleClose` that always resets `subject` / `body` / `editedRecipient` (fixes the stale-edit leak too — also applies to the `×` button and overlay click). `InterviewQuestionsModal#sendPackToHM` passes `onBack: () => {}` (no-op — closing the composer is enough since the questions modal is still mounted underneath) and `backLabel: 'Back to Interview Pack'`. Disabled the Back/Cancel buttons while a send is in flight to prevent stranding the modal mid-request |
| 2026-04-27 | "Hand Off to HM" on shortlisted cards sent a bare evaluation summary with no meeting details and no interview prep — meanwhile the "Generate Interview Questions" entry point lived on the CV Evaluation detail modal, where it didn't logically belong (you wouldn't prep questions for someone you haven't decided to interview). The two flows produced separate HM emails for the same candidate, neither of which carried the full picture | Two unrelated buttons evolved in parallel: `Shortlist.jsx#handOffToHM` opened EmailComposer directly with `getCandidateHandoffTemplate`; `EvalDetailModal.jsx` had its own "Generate Interview Questions" → InterviewQuestionsModal → "Send Pack" path. Result: HR was either sending two emails or skipping the prep step entirely | Chained the flow: clicking "Hand Off to HM" now opens `InterviewQuestionsModal` (state lifted to `Shortlist.jsx#interviewPrep`). HR fills meeting / generates questions / both / neither, then "Send Pack to Hiring Manager" opens EmailComposer. Extended `getInterviewPackTemplate` to take an `evaluation` arg and prepend the eval-summary block (so the pack-only path is at least as informative as the old handoff-only path). Added `onPackSent(candidateId, newEntry)` callback so Shortlist still patches its `emailMap` immediately after send. Removed the "Generate Interview Questions" button + InterviewQuestionsModal mount from `EvalDetailModal.jsx` — interview prep is no longer reachable from CV Evaluation. `getCandidateHandoffTemplate` left in `services/email.js` (unused by UI) in case a future consumer wants the bare-summary shape. Updated rules §12a, §12b, §18 |
| 2026-04-27 | Required-criteria items HR added in the criteria editor were sent to Ollama and parsed correctly into `required_missing` / `items_met` arrays, but the Detail modal only surfaced them as a paragraph at the bottom of the Evaluation Method block — easy to miss, no visual chips, no "X/Y met" summary | The n8n parser (`Eval - Score Candidates`) appends "Required items NOT met: a; b" and "Required items met: x; y" lines to the `reasoning` text column. The frontend dumped the entire reasoning string into a `<p>` near the bottom of the modal, which buried the most actionable info under the AI's prose | Added `extractRequiredItems(reasoning)` helper to `EvalDetailModal.jsx` that regex-matches both lines, splits on `;`, and returns `{missing[], met[], cleanedReasoning}`. New `RequiredItemsBlock` component renders red ✗ chips for missing and green ✓ chips for met, with a "{N} met · {M} missing" header, positioned right after the overall-score header and before Score Breakdown so it's the second thing HR sees. The Evaluation Method paragraph below now uses `cleanedReasoning` so the lines aren't shown twice. No backend / schema change — the data was already there |
| 2026-04-27 | Shortlist tab defaulted to the "All" filter, which mixed shortlisted (active pipeline) candidates with rejected ones at the top. Since the very first card a user usually saw was a rejected candidate, the page felt noisy and didn't reflect what HR is actually working on day-to-day | `useState('all')` for `slFilter` was the original default — chosen for symmetry with CV Evaluation Results' "All", but the two tabs serve different purposes. Results = "everyone who applied"; Shortlist = "everyone who's still in play". Defaulting to All on the latter shows decided-and-out candidates as prominently as active ones | Changed `useState('all')` → `useState('shortlisted')` in `Shortlist.jsx`. The pill bar still includes All so HR can switch back any time, but the landing view is the active pipeline. Rule §12 updated |
| 2026-04-27 | Rejected candidates in CV Evaluation Results had no way to undo the decision — the only escape was Archive (which hides them) or manually deleting the shortlist row. Same applied to shortlisted-but-not-yet-interviewed candidates: the existing "Unshortlist" button only handled the `shortlisted` case with a single confirm copy and toast | `unshortlistCandidate(candidateId)` was hardcoded to one path: confirm "Remove from shortlist", call `/remove-from-shortlist`, show "Candidate unshortlisted". No way to surface a different copy for the rejection-revert case, and no second button on the rejected-status branch | Renamed `unshortlistCandidate` → `revertDecision(candidateId, kind)` where `kind` is `'shortlist'` or `'reject'`. The function picks the confirm prompt and toast message based on `kind`, but otherwise uses the same `/remove-from-shortlist` endpoint (the row contains the verdict; deleting it returns the candidate to pending regardless of which verdict was set). Added an "Unreject" button to the rejected-status branch in CVEvaluation.jsx (around line 900). Updated the existing Unshortlist button to call `revertDecision(c.id, 'shortlist')`. Both reuse `setRetainedInView` + `setRecentlyChanged` so the row stays put with a brief animation instead of disappearing immediately |
| 2026-04-27 | Per-item weights (`wt` 0–100 input next to each criteria row) were a useless knob — the AI prompt only sent them as raw context with no normalization, no sum constraint, and no actual influence on scoring beyond the existing `[REQUIRED]`/`[optional]` flag | The original design intended weights to convey "how much this single item matters within its category" but the prompt format `[REQUIRED] (weight 30) ...` left interpretation entirely to the model — qwen3:4b basically ignored the number. The number input also let users type any value with no upper bound enforced by anything meaningful, leading to legitimate user confusion | Dropped the per-item weight entirely. UI grid is now `1fr auto auto` (text + Required checkbox + remove); footer summary is "N required · M optional" (no total weight). Save / runEvaluation / evaluateOne payloads omit `weight`. `criteriaItems` state shape is `{id, text, required}`. Backend prompt format dropped `(weight X)` — items are now `[REQUIRED] item.text` or `[optional] item.text`. `criteria_items` JSONB rows continue to deserialize cleanly because old rows have a `weight` field that's just ignored on load. Patched `workflows/phase2-cv-evaluation/phase2-cv-evaluation.json` source AND live n8n sqlite (`workflow_entity` + `workflow_history`) so the change takes effect on the next workflow execution after n8n restart |
| 2026-04-27 | When the "Save this criteria set" checkbox was on but the user clicked Continue without typing a name, the app threw a native `prompt('Enter a name for this criteria set:')` dialog — ugly, modal, and out of style with the rest of the UI | Originally added as a quick fallback when the inline name input was empty. `prompt()` blocks the page, has zero styling, and feels like a dev shortcut, not product UX | Replaced with inline validation: the existing name input gets a red border + small `Required when "Save this criteria set" is checked.` helper text below it, the field is auto-focused on the failed Continue, and a toast says "Enter a name for this criteria set first". Typing into the field clears the error state. New state: `criteriaNameError` boolean + `criteriaNameRef` for focus |
| 2026-04-27 | "From Job Description" button on Set Criteria said "This job has no description" even when the JD was clearly stored in Postgres (e.g. a fresh AI-generated 3000+ char JD) | The list endpoint `/job-openings` (Phase 2 workflow) intentionally projects only the summary columns and **does not** return `job_description` — the column is only included by the singular `/job-opening?id=N` endpoint. `CVEvaluation.jsx#selectJob` stored the list-row object as-is into `evalSelectedJob`, so `evalSelectedJob.job_description` was always `undefined` regardless of DB state | `selectJob(job)` now fires `/job-opening?id=N` in parallel with `fetchJobState`, and merges the full record into `evalSelectedJob`. The list endpoint stays lean (no extra payload across all jobs); the per-job detail is fetched lazily on selection. Fixes "From Job Description" + the auto-prefill in `goStep(2)` (line 138). Same bug affected anything else reading `evalSelectedJob.job_description` from list-only data |
| 2026-04-27 | "Hand off to HM" was a one-shot action with no pipeline footprint — once HR sent the pack the candidate looked identical to any other Shortlisted/Interviewed card, the email banner showed the misleading internal type "recommendation", and Hire/Reject were exposed too early (on Shortlisted/Interviewed cards), implying HR could decide before HM weighed in. There was also no way to filter the active-pipeline view down to "candidates currently with HM" | (a) Email banner pulled the raw `email_type` and rendered "recommendation" with the same green tint as a successful candidate-facing email — visually indistinguishable from "Email Shortlist" being sent. (b) No card-level signal that the candidate was post-handoff. (c) `shortlist.status` only had {shortlisted, interviewed, hired, rejected} so "with HM" had no native representation. (d) Hire/Reject lived on Shortlisted+Interviewed cards because that's the only place they had ever been | Added a derived "Handed off to HM" stage with no schema migration — `isHandedOff(candidateId)` is true when at least one `email_log` row with `email_type='recommendation'` and `status='sent'` exists for them and they haven't reached a terminal verdict. Shortlist tab gains a "Handed off" filter pill (between Interviewed and Hired) plus matching stat card. Shortlisted/Interviewed pills exclude handed-off candidates so the same person isn't double-counted across stages. Card visual: blue tint (`.candidate-card--handed-off`), blue "✉ Handed off to HM" chip in header, email banner switches to blue (`.sl-email-handoff`) when latest email is `recommendation`. Action layout: Hire/Reject removed from Shortlisted+Interviewed cards and relocated to Handed off cards alongside Send Offer + Re-send Pack. Centralized `email_type` → user-facing label in `utils/helpers.js#emailTypeLabel(type)`; both Shortlist and Emails pages use it, so "recommendation" never reaches the user — they always see "Handed off to HM". Hired/Rejected pills still take from `shortlist.status` so terminal verdicts trump handoff state |
| 2026-04-27 | Asking for N interview questions returned fewer (e.g. user requested 3, got 2). Bug compounded as request size grew | `IntQ - Parse Response` (Phase 3 workflow) hard-filtered each question by exact category match against the requested set (`hr` / `technical` / `salary`). qwen3:4b is loose with category labels — it routinely emits `behavioral` / `behaviour` / `cultural fit` / `soft skills` for HR-style questions and `compensation` / `pay` / `comp` for salary, occasionally `tech` / `engineering` for technical. Any question with a non-canonical label was silently dropped, so the response shrank to 2 (or 1) instead of the 3 the user asked for. The Build Prompt node already capped output at `numQuestions`, so once the LLM emitted N items, dropping any of them couldn't be made up later | Added an `ALIASES` table to `IntQ - Parse Response` mapping common qwen3 variants → canonical (`behavioral`/`behaviour`/`soft`/`cultural`/`motivation`/`personality` → `hr`; `tech`/`skills`/`engineering`/`coding` → `technical`; `compensation`/`pay`/`expected salary`/`comp` → `salary`). Categories are lowercased + trimmed before lookup. Truly unknown categories now fall back to the **first requested** category (`fallbackCategory = ctx.includeHr ? 'hr' : ctx.includeTechnical ? 'technical' : 'salary'`) instead of being dropped — better to mislabel one question than return fewer than requested. Also added a defensive `.slice(0, ctx.numQuestions)` in case the LLM over-generates. Patched in `phase2-cv-evaluation.json` source AND live n8n sqlite (`workflow_entity` + `workflow_history` per the documented protocol — see 2026-04 row above), then bounced n8n. Smoke-tested: requesting 3 returns exactly 3 (one HR / one technical / one salary) |
| 2026-04-27 | HM replies to a candidate-pack email never made it back into the app — HR had to bounce to Outlook/Gmail to read them, and there was no way to tie a reply back to the candidate it concerned. Same applied to candidate replies on rejection / interview-invite / offer threads | Outbound was a one-way pipe. The SMTP sidecar relayed messages but didn't persist anything that could be matched against a future inbound. There was no IMAP poller, no `/inbound-email` webhook, and `email_log` had no notion of direction or threading headers. Even if we had pulled inbound mail somewhere, the FK constraints (`candidate_id` + `job_opening_id` NOT NULL) meant we'd have nowhere to insert orphan replies | Built a 4-layer pipeline. (1) Migration `009-email-direction.sql` adds `direction` (CHECK outbound/inbound, default outbound — backfills cleanly), `message_id`, `in_reply_to` columns + indexes on both ID columns for O(log n) reply matching. (2) `scripts/smtp_server.py` now generates an RFC-822 Message-ID via `email.utils.make_msgid(domain=...)` (domain taken from the sender's `@`-suffix so Gmail doesn't rewrite it), sets it on the MIME envelope, and returns it in the JSON response; Phase 5's `Send - Log Result` query persists it as `message_id` on the outbound row. (3) `scripts/imap_server.py` (new) is a polling daemon on `127.0.0.1:8902` — `IMAP4_SSL` login, `SEARCH UNSEEN`, decode RFC-2047 headers, walk multipart for plain text (HTML fallback with tag stripping), strip reply-quote tails, POST to n8n's new `/inbound-email` webhook, mark `\Seen` only after successful forward (failures stay UNSEEN so the next poll retries). Same Gmail App Password as SMTP works. Without `IMAP_HOST`, the daemon stays running but skips polling — symmetric with the SMTP sidecar's "logged only" mode. (4) Phase 5 `/inbound-email` webhook chain: validate → drop cold inbounds (no In-Reply-To / References — nothing to attach to) → look up parent via `email_log.message_id = $in_reply_to` (last token of References as fallback) → orphan check → INSERT direction='inbound' row inheriting parent's candidate_id/job_opening_id with sender in `recipient_email` (column reused as "the other party" per migration comment). Orphans get a 200 with `{orphan: true, reason}` so the IMAP sidecar marks Seen and stops re-forwarding. Frontend: Shortlist card banner gets purple tint + 📥 + "Reply from {sender}", detail panel uses "From:" label + Inbound badge, Emails table gets dedicated "inbound" filter tab + stat card + purple row background + "from" prefix on recipient cell. `isHandedOff()` and `hasEmailSent` both filter out inbound rows so receiving a reply doesn't toggle pipeline state. `sentCount`/`failedCount` exclude inbound (they only count outbound delivery). Wired `python scripts/imap_server.py` into `start.sh` between SMTP and n8n; added IMAP_* env block to `.env.example`. Patched live n8n sqlite via `scripts/patch_imap_inbox.py` (workflow_entity + workflow_history per protocol) and bounced n8n. Smoke-tested end-to-end: orphan path returns `{orphan: true}`, cold-inbound path returns `{dropped: true}`, threaded path inserts the inbound row with the parent's candidate_id |
| 2026-04-26 | Two frontends in the same repo caused real confusion — user repeatedly opened the legacy single-file SPA (port 3000) instead of the React app (port 3001), then reported that newly added React features (e.g. "Hand Off to HM" button) were "missing". Both ran from `start.sh` so it was easy to land on the wrong one | Legacy `frontend/index.html` (3526-line monolith) was preserved as a "fallback" but was actually being kept in sync only when convenient — it had drifted significantly from the React app, didn't have any of the recent features (interview-question generator, recommendation email, candidate handoff, meeting metadata, etc.), and `npx serve -l 3000` in `start.sh` made it look first-class | Deleted `frontend/index.html` and `frontend/server.js`. Removed the `[5/6] Starting legacy frontend` block from `start.sh` and renumbered the React step to `[5/5]`. Removed the "Legacy App: http://localhost:3000" line from the launcher output. Updated README.md (stack table, project tree, startup description) and CLAUDE.md (modules table, architecture section, openEmailComposer reference) to drop all legacy mentions. **Rule going forward:** there is one frontend — `frontend-react/`. Never re-introduce a parallel HTML SPA. The empty `frontend/` directory may persist briefly because OneDrive holds a sync lock on it; it has no contents and will be reclaimed by sync |
| 2026-06-09 | New job openings appeared with `status = 'draft'` — they showed in the DB but were filtered out of the active-pipeline view and couldn't be toggled active | n8n `Create - Prepare DB Insert` Code node had `status: item.status \|\| 'draft'` fallback. The browser never sends a `status` field on create (status is server-assigned), so every new job got `'draft'` instead of the intended `'open'` | Changed fallback to `status: item.status \|\| 'open'` in the n8n Code node. Patched live sqlite in both `workflow_entity.nodes` and `workflow_history.nodes` (dual-table protocol). Backfilled existing draft rows: `UPDATE job_openings SET status='open' WHERE status='draft'` |
| 2026-06-09 | Toggling a job active/inactive flipped `is_active` but the `status` column stayed stale (showed `'open'` even when deactivated, or `'draft'` after re-activation). The status pill in the UI was always wrong | `Toggle - Validate ID` SQL only did `SET is_active = NOT is_active WHERE id = N` — never touched `status`. Additionally the SQL string used JS single quotes, so the CASE expression literals `'open'` and `'closed'` terminated the outer string early, silently crashing the node (returned HTTP 200 with empty body) | Rewrote the SQL as a JS template literal so embedded single quotes are no longer an issue. Added `status = CASE WHEN NOT is_active THEN 'open' ELSE 'closed' END` to the UPDATE. Patched both workflow tables in sqlite |
| 2026-06-09 | QBank "Edit" (PUT) returned an empty body and never saved changes to the question bank | n8n `QBank - Update` WHERE clause used `$json.body.id`, but the frontend sends the question ID as a URL query param (`?id=N`), not in the POST body — so the WHERE matched nothing | Changed to `($json.query && $json.query.id) \|\| $json.body.id` in the Postgres node. Patched both workflow tables |
| 2026-06-09 | Candidate interview crashed mid-session when advancing to the next question — page went blank with a ReferenceError | `CandidateInterview.jsx` line 609 called `speak(q.question)` but `q` was never declared in that scope; the next question was stored in the variable `nextQ` | Changed `speak(q.question)` → `speak(nextQ.question)` |
| 2026-06-09 | Sent emails (shortlist notification, rejection, HM pack) appeared as "Inbound" in the Emails tab and didn't count toward `isHandedOff()` / `hasEmailSent` derived state | Three `newEntry` objects constructed locally after a successful send were missing the `direction` field. Without it, the `e.direction !== 'inbound'` guard treated them as inbound, breaking all downstream logic | Added `direction: 'outbound'` to: (1) `sendEmail` newEntry in `Shortlist.jsx:137`, (2) rejection newEntry in `Shortlist.jsx:162`, (3) pack-sent newEntry in `InterviewQuestionsModal.jsx:219` |
| 2026-06-09 | `question_bank`-related endpoints returned 500 / table-not-found errors; "Save to Bank" and QBank page were completely broken | Migration `013-question-bank.sql` had never been applied to the running `hr-postgres` Docker container — the table simply didn't exist | Applied migration: `docker exec -i hr-postgres psql -U hr_admin -d hr_automation < db/migrations/013-question-bank.sql` |
| 2026-06-10 | Mic stopped working after the first interview question — Q2 and Q3 always captured "(no response)" | `rec.onend` restart logic didn't check `speakingRef.current`. After Q1, `advanceQuestion` aborted recognition and set `loadingQRef=true`. Loading finished before TTS started speaking Q2, so when `onend` fired with `loadingQRef=false`, it scheduled a 300ms restart that fired *during* TTS. Then the explicit `startRecognition()` call after TTS aborted that premature instance, firing another `onend`, cycling until Chrome threw `'service-not-available'` and permanently killed the mic | Added `&& !speakingRef.current` to both the outer `if` and inner `setTimeout` check in `rec.onend` in `CandidateInterview.jsx` |
| 2026-06-10 | Handed-off cards had a blue background tint while shortlisted cards were plain white — visually inconsistent. Reject button missing on shortlisted cards | `candidate-card--handed-off` had `background:#eff6ff` with blue border/shadow. Reject was only wired to `interviewed` and `handedOff` blocks, not `shortlisted` | Stripped background/shadow from `candidate-card--handed-off` (kept the blue left-border accent as the only visual marker). Added `✗ Reject` button to the shortlisted actions block in `Shortlist.jsx` |
| 2026-06-10 | Email Shortlist / Interview Invite / Send Offer buttons missing for candidates with no email address | `hasEmail = s.email && s.email.includes('@')` gated all three buttons — candidates uploaded without an email (e.g. CV-only uploads) had zero email actions available | Removed `hasEmail &&` guards. When `email` is missing, `sendEmail()` now passes `editableRecipient: true` so the composer opens with an empty, typeable To: field. `onSend` reads `resolvedEmail` from the composer instead of the captured `email` closure variable |
| 2026-06-10 | Interview with 2/3 "(no response)" answers scored 7.0 overall — model was far too lenient | Two root causes: (1) the example JSON in the prompt had hardcoded `7` for every field, biasing the model toward 7 regardless of actual answers; (2) no penalty rule for silent answers, so the model extrapolated a 7 from one decent Q1 answer and ignored the two silences. Patched in `scripts/patch_interview_eval_prompt.py` | Rewrote `IntEval - Build Prompt`: removed hardcoded example values (replaced with `<score>` placeholders), added dynamic `penaltyInstruction` block that computes `noResponseCount` at runtime and injects hard caps (≥50% unanswered → overall ≤ 3; any unanswered → overall ≤ 5), and added rule that overall = average of the 4 dimensions. Patched both `workflow_entity` and `workflow_history` in live sqlite |
| 2026-06-10 | AI interview evaluation showed 0.0 for all scores with no way to re-run it from the expanded card view | `isPending()` checks `!s.scoreOverall` — if the evaluate call completed but returned 0s (thin transcript from the mic bug), the card exits pending state and the Re-evaluate button disappears. HR had no way to trigger a fresh evaluation without manually editing every score | Added "↻ Re-evaluate with AI" button to the expanded detail section in `AIInterviews.jsx`, always visible regardless of pending state |
| 2026-06-10 | n8n failed to start from `launch.bat` — `Cannot find module '../package.json'` crash | `start.sh` used `npx n8n start` and the PATH put `/d/n8n/node_modules/.bin` first. That local install is corrupt (missing `package.json`). The working global install lives at `C:\Users\Jasse\AppData\Roaming\npm\n8n` (v2.16.1) but `npx` resolved the broken local one first | Changed `npx n8n start` → `n8n start` in `start.sh`. Moved global npm path (`/c/Users/Jasse/AppData/Roaming/npm`) before `/d/n8n/node_modules/.bin` in the PATH export so the global install always wins |
| 2026-06-10 | `InterviewQuestionsModal` had only AI Generate as a question source — no way to pick from the question bank or write custom questions. AI-generated questions also showed in a separate preview list that had to be manually added with "Add All", so clicking "Send Pack to HM" with questions visible in the preview sent an empty email | (a) Only one question source existed. (b) AI generate wrote to `aiPreview` state, not `questions` state — "Send Pack to HM" read from `questions`, so the preview was invisible to the email template. (c) No connection between `question_bank` table and the interview prep flow | Full redesign of `InterviewQuestionsModal`: 3 source tabs (From Question Bank / AI Generate / Write My Own) with image-13-style card selector. Bank tab: mini picker (search + category chips + checkboxes + "Add Selected"). AI tab: `generate()` now calls `addQuestions(qs)` directly — no `aiPreview` state, no extra add step — so generated questions are always in the unified list and always in the email. Own tab: category select + textarea + Add. Per-question "→ Bank" button saves to shared bank. "Save to Profile" persists to both localStorage and `candidate_prepared_questions` DB table |
| 2026-06-10 | Live Interview had no way to see which candidates had prepared questions or load them for an upcoming interview without re-entering everything | Questions saved in `InterviewQuestionsModal` wrote to `candidate_prepared_questions` DB table (migration 014) but nothing read from it in the Live Interview flow | Added "Candidate Prep" third sub-tab to `LiveInterview.jsx`. Fetches `/candidate-questions-list`. Expandable candidate cards show question count + meeting info + last-saved date. "Use for Interview" switches to Setup tab, auto-selects the candidate via `pendingPrep` state + `useEffect` on candidates load, and populates `generatedQs` with the saved questions. Added 3 new n8n endpoints to workflow 6 via `scripts/patch_candidate_questions.py`: POST/GET `/candidate-questions` + GET `/candidate-questions-list`. DB migration 014 applied |
| 2026-06-11 | Interview video recordings were never saved — every `interview_sessions.recording_path` was empty and the recordings dir had 0 files | `finishInterview()` calls `stopCamera()` which stops the MediaRecorder; `stopAndUploadRecording()` then early-returned on `rec.state === 'inactive'` before ever building the upload blob. The captured chunks were sitting in `recordingChunksRef` the whole time | `stopAndUploadRecording` (CandidateInterview.jsx) now only awaits `rec.stop()` if the recorder is still active, then uploads from `recordingChunksRef` regardless of recorder state |
| 2026-06-11 | Candidate edits made on the review screen ("Tap to edit before submitting") were silently discarded — the original mis-transcribed answers got submitted and scored | Textarea edits updated `completedPairs` state, but `submitInterview()` read `transcriptRef.current`, which was never synced from those edits | `submitInterview` now submits `completedPairs` (and syncs `transcriptRef` to it) so the reviewed answers are what gets saved and evaluated |
| 2026-06-11 | "Edit Evaluation Manually" zeroed all five scores on save (summary/recommendation survived) | `ManualEvalForm` sent `{scoreComm, scoreTech, scoreConf, scoreCulture, scoreOverall}` but the n8n `IntTx - Prep` node reads `scores.communication/technical/confidence/cultureFit/overall` — every field parsed as `undefined → 0` | Manual save (AIInterviews.jsx) now sends the backend key names, plus `perQuestion` and `requirementsMatch` from the existing session so those aren't wiped either. Re-evaluate also passes `requirementsMatch` through |
| 2026-06-11 | Expected-answer rubrics typed in the Setup tab never reached the AI evaluator, and the Requirements Check panel was permanently empty for new interviews | Two causes: (a) `generateLink()` (LiveInterview.jsx) stripped `modelAnswer` when building `customQuestions` for the link token, so `IntEval - Build Prompt`'s `modelAnswerMap` was always empty; (b) `patch_interview_eval_prompt.py` (the 2026-06-10 leniency patch) overwrote `IntEval - Build Prompt` wholesale and dropped the requirements-extraction block that `patch_wf6_recording.py` had added. Additionally `IntTx - Save` clobbered `requirements_match` with `[]` on every re-save | `generateLink` now includes `modelAnswer` in all three source branches. New `scripts/patch_qa_fixes.py` merges the leniency rules + requirements extraction into one `IntEval - Build Prompt` (also reads `body.durationSeconds` — the field the frontend actually sends; the prompt previously always said "0 minutes"), and gives `requirements_match` the same CASE-preserve treatment as `recording_path` in `IntTx - Save`. Applied to live sqlite (entity + history) and verified end-to-end: evaluate with a salary rubric returns real scores + extracted requirements |
| 2026-06-11 | Shortlist "View Profile → Email Recommendation to HM" logged the email against the wrong candidate with `job_opening_id=undefined` | `Shortlist.jsx` passed the raw shortlist row to `EvalDetailModal` — there `c.id` is the shortlist row id, not the candidate id (verified: row had id=3, candidate_id=1) — and passed no `job` prop at all | The modal now receives `{...row, id: row.candidate_id}` and the selected job object |
| 2026-06-11 | LiveInterview's "saved questions" server fallback always returned [] — saved prep never loaded on a fresh browser | The fallback read `/interview/saved-questions` over the `interview_questions` table (migration 010), which **no workflow ever writes to** (0 rows since creation). The real prep store is `candidate_prepared_questions` (migration 014) | Fallback rewired to `GET /candidate-questions?candidate_id&job_id` (CQ Get). Same fix applied to `InterviewQuestionsModal`, which per §12b was documented to fall back to the DB but only ever read localStorage. `interview_questions` + `/interview/saved-questions` remain for now as dead-but-harmless; candidates for removal |
| 2026-06-11 | Candidate Prep tab never showed meeting details or general notes on its cards | `CQ List - Validate` SQL didn't select `general_notes`/`meeting`, but the tab rendered both fields | Columns added to the query (via `scripts/patch_qa_fixes.py`). Also relabeled the tab's misleading "— All jobs —" filter option to "— Select a job —" (empty value loads nothing by design) |
| 2026-06-11 | Re-importing repo workflow JSONs would have silently broken the Question Bank tab, Candidate Prep tab, Save to Profile, and the SMTP health pill | Live workflows are patched in place by `scripts/patch_*.py`, but the repo JSONs were never re-exported — phase6 was missing the 4 `interview/question-bank` + 3 `candidate-questions*` endpoints, phase4 was missing `smtp-health`, and all node code was pre-patch | New `scripts/export-live-workflows.py` dumps live `workflow_entity` nodes+connections into the repo JSONs. **Rule: run it after every live workflow patch.** All 6 repo JSONs now match the running instance |
| 2026-06-11 | n8n editor password was committed in plain text in claude.md / docs/n8n.md / docs/runbook.md — and the repo is public on GitHub | REST-toggle examples were written with literal credentials | Docs now reference `$N8N_REST_USER` / `$N8N_REST_PASSWORD` from `.env` (gitignored; keys added to `.env.example`). **The old password must be treated as compromised — it remains in git history. Rotate it.** Also noted: on n8n 2.16+ with user management disabled, `/rest/login` 404s — webhooks self-register ~15-30s after `/healthz` turns ok, so wait-and-retry instead of REST-toggling |
| 2026-06-11 | Interview links sent to candidates didn't work — dead page on open, and even when the page loaded remotely, answers/recordings never reached the HR machine | Three compounding causes. (a) The token was raw `btoa()` output whose alphabet includes `/` — a slash inside the token splits the URL path so the `/interview/:token` route stops matching (verified: a 2-question token with rubrics is ~500 chars and virtually always contains `/`). (b) Vite 8's `allowedHosts` rejects any non-localhost Host header, so opening the link through a Cloudflare tunnel got "Blocked request". (c) The candidate's browser called `http://localhost:5678/webhook` and uploaded recordings to `http://localhost:8903` — *their* localhost, not the HR machine's | (a) Tokens are now URL-safe base64 (`-`/`_` for `+`/`/`, padding stripped) — `encodeInterviewToken()` in LiveInterview.jsx, matching restore in `decodeToken()` (CandidateInterview.jsx). (b) `vite.config.js` sets `allowedHosts: true`. (c) All candidate-path calls are now relative and same-origin: `VITE_API_URL=/webhook`, recording upload/playback use `/recording/...`, and the vite dev server proxies `/webhook`→:5678 and `/recording`→:8903. `api.js` default fallback changed to `/webhook`. Link origin = wherever HR opened the app (`VITE_PUBLIC_URL` env can override). **Operational rule: tunnel port 3001 only** (`cloudflared tunnel --url http://localhost:3001`), open the app through the tunnel URL, generate the link there — and never tunnel 5678 directly (that exposes the auth-disabled n8n editor publicly). See docs/runbook.md §Remote candidate interviews |
| 2026-06-11 | **Feature: Talent Pool tab.** HR had no way to search across ALL uploaded CVs ("does anyone in our pool know Power BI?") — CVs were only visible per-job in CV Evaluation | New requirement, not a bug | New `/talent-pool` nav tab + route. `GET /talent-pool` endpoint added to WF5 (Talent - Webhook → Fetch → Response; via `scripts/patch_talent_pool_attachments.py`) returns every candidate with cv_text + job + eval + shortlist status. Search is plain client-side text matching (Ctrl+F semantics: space-separated terms, ALL must match across name/email/job/CV text) with `<mark>` highlighted snippets (±60 chars per hit), expandable full CV text with highlights, View PDF, and a Shortlist action (adds to the job they applied for). No AI involved by design |
| 2026-06-11 | **Feature: Teams interview import.** Interviews held on Microsoft Teams had no way into the system — no transcript, no AI evaluation | New requirement | "⬆ Import Teams Transcript" button on AI Interviews. Parses Teams' exported `.vtt` (cues with `<v Speaker>` tags), groups consecutive cues into speaker turns, HR picks which speaker is the candidate, interviewer turns become `question` / candidate turns become `answer` (the exact shape IntEval + the transcript UI already use), then save-transcript → `/interview/evaluate` → save scores. Per-turn `t` (seconds) is kept so the overlay also works if a recording is attached later. All client-side parsing — no new sidecar |
| 2026-06-11 | **Feature: recording consent + synced question overlay.** The recording only captured the candidate's camera+mic — the AI's spoken (TTS) questions are not in the audio, so HM playback was answers-with-no-questions. No recording consent was shown either | TTS plays through speakers; `speechSynthesis` audio cannot be routed into MediaRecorder | Each transcript entry now carries `t` = seconds-into-recording when its question was asked (`recordingStartRef`/`currentQStartRef` in CandidateInterview.jsx — rides inside the transcript jsonb, **no schema change**). New `RecordingPlayer` in AIInterviews overlays the current question on the video synced via `onTimeUpdate`; old recordings without timestamps show a subtle note instead. Intro screen now states the interview is recorded and that starting = consent |
| 2026-06-11 | **Feature: Email HM from AI Interviews with selectable attachments.** No way to send interview results to the hiring manager, and the email pipeline couldn't attach files at all | SMTP sidecar built MIMEText only; `/send-email` had no attachment fields | Shared `EmailComposerModal` gains an optional `attachmentOptions` checkbox section (any combination selectable). "✉ Email HM" on each session opens it with: PDF interview report (generated client-side via **jspdf** — `utils/pdfReport.js`: scores, summary, requirements, transcript), candidate CV (when on file), interview recording. Plumbing: small files travel base64 through `/send-email` → sidecar (`attachments[]`); the recording goes by **filename only** and the sidecar reads it from `recordings/` itself, so large videos never hit n8n's payload limit (`recording_file`). Sidecar builds MIMEMultipart, skips anything putting the total over 18 MB and reports `attachments_skipped`; email_log body gets an `[Attached: …]` suffix (WF4 validate/log nodes patched). Verified end-to-end with a real send |
| 2026-06-11 | Talent Pool UI iterations per user feedback: (a) "Read CV" expanded raw extracted text — user wanted the actual PDF inline; (b) action buttons misaligned across rows (View CV shifted left when Shortlist was present); (c) tab label/position felt wrong | First-pass design choices | (a) "View CV" now drops down the **original PDF in an inline iframe** with a header bar + "Open in new tab ↗" (same pattern as the Results CV panel); blob URLs cached per candidate; candidates without a stored PDF fall back to highlighted text, labeled as such. (b) Actions are a fixed two-slot grid (116px × 36px each): slot 1 always View CV, slot 2 always filled — Shortlist button or the status pill (moved out of the name line) — so buttons align vertically on every row. (c) Renamed **CV Pool**, moved to 2nd nav position right after Dashboard. Rows restyled: avatar initials, score block, amber snippet quotes, active-card border highlight |
| 2026-06-11 | Nav had 8 tabs; AI Interviews logically belongs inside the interview flow | User request | **AI Interviews merged into the Interview tab** (Live Interview renamed "Interview") as a 4th sub-tab **Results**. `AIInterviews.jsx` gains an `embedded` prop (skips its own container/heading) and is rendered by `LiveInterview.jsx`; `/ai-interviews` now redirects to `/live-interview?tab=results` (the Interview page reads `?tab=` on mount for deep-linking). Nav is 7 tabs |
| 2026-06-14 | QA pass against the interview feature checklist found the model-answer/requirement scoring path **broken through the post-shortlist prep flow**: model answers + requirement scoring worked from the Interview **Setup** tab, but `InterviewQuestionsModal` (the Shortlist hand-off prep modal) only offered hr/technical/salary categories and stored expected answers as display-only `hints` — which never became the `modelAnswer` the eval scores against, and didn't persist. So "use prepared questions after shortlist" + "HR gives model answers" + "evaluate on salary/iqama/requirements" did not compose through that path | The prep modal predated the rubric/requirements work and was never reconciled with it | Added the four requirement categories (iqama / notice / location alongside salary) + their colors/order to `InterviewQuestionsModal`. Each prepared question now has an editable **"Expected answer"** field bound to `q.modelAnswer` (labeled "/ requirement" for requirement categories). Bank picker maps `b.modelAnswer → modelAnswer` (was `→ hints`). `saveProfile` already persists the `questions` array, so `modelAnswer` now survives into `candidate_prepared_questions`; the Interview Setup tab already reads `q.modelAnswer` on load (line ~154) and forwards it in the link token (line ~214), so it reaches `IntEval` for rubric scoring + requirements extraction. Word export / Copy relabeled "Hint" → "Expected answer". Verified the modelAnswer roundtrip through `POST`/`GET /candidate-questions`. **Result: questions chosen/prepped after shortlist now carry HR model answers all the way to the AI score.** |
| 2026-06-14 | User asked to swap nav order of Job Openings and CV Pool | Preference | Reordered `tabs` in NavTabs.jsx → Dashboard · **Job Openings · CV Pool** · CV Evaluation · Shortlist · Interview · Emails (later also swapped Emails/Interview). Final order: Dashboard · Job Openings · CV Pool · CV Evaluation · Shortlist · Interview · Emails |
| 2026-06-14 | **Rejecting a candidate who has no email on file silently failed to send/log the rejection email.** The composer correctly lets HR type a recipient for a candidate with no stored email, but both reject handlers (`CVEvaluation#rejectCandidate`, `Shortlist#rejectFromShortlist`) called `sendEmailRequest` with the **empty closure `email`/`s.email`** instead of the address resolved by the composer. WF4 rejected the blank `recipient_email` as invalid (no log row written), and the handler showed "Candidate rejected" regardless — so it looked sent. Confirmed: 5 rejected candidates, 0 rejection rows in `email_log`; candidate 12 (no email) was the repro | Same closure-vs-resolved-email bug class fixed earlier for the shortlist *send* buttons, but missed in the *reject* handlers | Both `onSend` callbacks now destructure `recipientEmail: resolvedEmail`, send to `resolvedEmail || <closure>`, gate on `sendEmail && to`, and toast the real send status (sent / logged / failed) instead of a blanket "rejected". Backend verified to log rejections fine (the bug was entirely client-side). Reject status still persists even when no email is sent |

---

## 9. Development Rules

- **No silent failures.** Every API call in the UI has error handling. Every failure surfaces a toast.
- **Preserve existing data.** Schema changes are additive migrations (`ALTER TABLE ADD COLUMN IF NOT EXISTS`), not destructive rewrites. Never drop a table in a migration.
- **Validate before save.** Server side via CHECK constraints, client side via explicit checks (weights sum to 100, valid email format, required fields).
- **Keep project tidy.** New files follow the existing structure: workflow → `workflows/phaseN-*/`, migration → `db/migrations/NNN-*.sql`, doc → `docs/*.md`.
- **Do not break working flows.** Before changing a wizard step or workflow, verify the other steps/workflows still function end-to-end.
- **Trust internal invariants.** Don't add defensive checks for cases that can't happen in this codebase. Only validate at external boundaries (user input, HTTP requests from the browser, Ollama output parsing).
- **Don't rewrite unrelated code** during a bug fix. A UI fix doesn't need adjacent JS refactored.
- **No code change without documentation update.** Every code change must be paired with the matching docs update in the **same commit**:
  - UI behavior change → update UX rules in `claude.md`
  - Table / modal structure → update `claude.md` UX rules **and** `docs/architecture.md`
  - Workflow logic → update `docs/n8n.md`
  - Database schema → update `docs/database.md` **and** list the migration in `claude.md` §3
  - Feature behavior → update the README feature summary and the relevant `docs/*.md`
  - Any bug fix → add a row to the Fix Log in `claude.md` §8
  If docs are outdated, the system is considered broken. Reviewer should reject PRs with code changes that leave docs stale.

---

## 10. Future Work

- **Better CV parsing.** Current PDF.js extraction is brittle on multi-column or scanned CVs. Could add OCR fallback or use a library like `pdf-parse` with server-side parsing.
- **Better scoring.** Currently one big prompt to `qwen3:4b`. Could split into per-dimension scoring, add structured output (JSON schema), use a larger model when available.
- **Email templates.** Right now rejection emails use a hardcoded template. Add a template manager with variables (`{{candidate_name}}`, `{{job_title}}`).
- **Analytics improvements.** Dashboard is basic counts. Could add funnel visualization (applied → scored → shortlisted → hired), time-to-hire, source-of-hire if that data were tracked.
- **SMTP test endpoints.** Sidecar needs `POST /test-connection` and `POST /send-test` for the Emails page buttons.
- **Multi-user auth.** Everything assumes one HR user on localhost. Adding real auth means enabling `N8N_USER_MANAGEMENT_DISABLED=false` and wiring a login.

---

## 11. Drive Layout (Canonical — D Drive Only)

**All runtime data lives on D:\**. Never look at E:\ for any of this — E had stale partial copies that caused repeated confusion and have been deleted.

| What | Path |
|------|------|
| n8n install + node_modules | `D:\n8n\` |
| n8n runtime data (.n8n folder) | `D:\n8n\.n8n\` |
| n8n SQLite database | `D:\n8n\.n8n\database.sqlite` |
| NodeJS runtime | `D:\NodeJS\` |
| Ollama models + program | `D:\ollama\` |
| Project source code | `D:\OneDrive\Desktop\Diyar\hr-automation\` |
| Docker WSL2 data | Docker default (managed by Docker Desktop) |

**start.sh PATH line:**
```bash
export PATH="/d/NodeJS:/d/n8n/node_modules/.bin:/c/Users/Jasse/AppData/Roaming/npm:$PATH"
export N8N_USER_FOLDER=/d/n8n
export OLLAMA_MODELS=/d/ollama
export OLLAMA_HOME=/d/ollama
```

**n8n credentials:**
- URL: http://localhost:5678
- Email: j.danaf@diyarme.com
- n8n user management is disabled via `N8N_USER_MANAGEMENT_DISABLED=true` — no login should be required. If it prompts for login, the env var wasn't picked up at startup; kill all node.exe processes and rerun `start.sh`.

**n8n workflow IDs (D drive database):**

| ID | Name |
|----|------|
| 1 | Phase 2 - Job Openings |
| 2 | Phase 3 - CV Evaluation |
| 3 | Phase 3 - Shortlist |
| 4 | Phase 4 - Email Notifications |
| 5 | Phase 5 - Dashboard |
| 6 | Phase 6 - Live Interview |

**If n8n webhooks return 404 after restart:**
The workflow is active in the DB but webhooks aren't registered in memory. Fix: authenticate via the REST API and toggle the workflow programmatically (see patch protocol below), or open http://localhost:5678 in the browser, find the workflow, and toggle it OFF then back ON.

**n8n REST API auth (for scripted toggles):**
```bash
# Login — get session cookie. Credentials live in .env (gitignored) as
# N8N_REST_USER / N8N_REST_PASSWORD — never commit them to this file.
# NOTE: on n8n 2.16+ with N8N_USER_MANAGEMENT_DISABLED=true, /rest/login
# returns 404 (no auth endpoints exist). In that case skip the REST toggle:
# webhooks register on their own ~15-30s AFTER /healthz turns ok — wait and
# re-test before assuming they're broken.
curl -c /tmp/n8n.txt http://localhost:5678/rest/login -X POST \
  -H "Content-Type: application/json" \
  -d "{\"emailOrLdapLoginId\":\"$N8N_REST_USER\",\"password\":\"$N8N_REST_PASSWORD\"}"

# Activate workflow (replace WF_ID and VERSION_ID)
curl -b /tmp/n8n.txt http://localhost:5678/rest/workflows/WF_ID/activate \
  -X POST -H "Content-Type: application/json" \
  -d '{"versionId":"VERSION_ID"}'
```
VERSION_ID comes from `workflow_history` (latest row for the workflow). **CRITICAL:** calling `/deactivate` via REST clears `activeVersionId` in `workflow_entity`. Always re-patch the DB (set `active=1, activeVersionId=<ver>`) after any deactivate/activate cycle, then call activate with the versionId.
